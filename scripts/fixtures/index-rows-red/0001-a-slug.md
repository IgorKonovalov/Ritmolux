# A decision whose roster row stays a pointer

A link target so `roster.md`'s under-cap row is shaped like a real roster row — link, title,
status — without seeding a break outside `doc-links/`, which `scripts/fixtures/README.md` forbids.
