<!-- translated-from: 1111111 -->

Установка коробки
=================

Короткая форма sha — семь знаков, ровно то, что печатает `git log -1 --format=%h`. Сравнение
выполняется по префиксу, поэтому короткий штамп проверяется на своей ширине, а не дополняется
нулями.
