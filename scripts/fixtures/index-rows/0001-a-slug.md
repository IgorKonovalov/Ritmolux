# A decision with a title of an ordinary length

A link target so `roster.md`'s rows are shaped like real roster rows — link, title, status —
without seeding a break outside `doc-links/`, which `scripts/fixtures/README.md` forbids.
