# Fixture root

## Through a diffusion model

A pointer to `tools/sd-filter/`, with the one allowed orientation figure - and it
quotes a number that is in no figures region of the canonical page.

<!-- figures:orientation --> A four-minute track takes about 99 minutes at the `fast` profile.
