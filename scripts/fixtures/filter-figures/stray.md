# A document that mentions the filter in one section

## An unrelated section

Preset reload takes ~150 ms and a horizon row lands every 30 s. Neither is in
scope, because this section does not name the tool.

## A section about `sd-filter`

The stage costs 1.354 s per diffused frame, which is a copy of a figure that
belongs on the canonical page.

```bash
# Fenced code is never scanned, so this 2.966 s is not a break.
python tools/sd_filter.py --profile quality
```

### A subsection under it

This inherits scope from its parent, but carries no figure.

## Another unrelated section

Back out of scope: 4.4 GB of nothing to do with it.
