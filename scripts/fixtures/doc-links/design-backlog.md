# A stand-in for docs/design-backlog.md

The fourth break class is a **form** rule, not a resolution failure (ADR-0149): the anchored link
in `broken.md` points at a file that exists, which is exactly why the gate reported 87 of them
clean. This file is that target. Without it the seeded link would break as class 1 as well and one
seeded line would produce two findings.

## 0001 — a heading, so the form being retired has something to have aimed at

An entry's heading text is its anchor, and that is the half that cannot survive: the close ceremony
moves a discharged entry's body to the archive and leaves a ledger row carrying the **number** and
not the heading.
