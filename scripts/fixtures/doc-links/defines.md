# The file that defines the orphaned label

This is the half that stays behind when a close ceremony moves link-dense prose into another
document. The definition resolves, so this file contributes no break of its own — its only job is
to put the label into the tree-wide known set, which is what makes the use in `broken.md` a lost
link rather than ordinary brackets.

[orphan]: exists.md
