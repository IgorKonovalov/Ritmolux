# A page that writes the roster down

All twelve systems have a palette, and the palette is where a preset's colour
comes from.

The 14 systems each declare their parameters through a `ParamSpec`, so the
reference is emitted rather than typed.

Every scene the converter reaches is a warp mesh; the other ten systems take
exactly the path they took before.

A preset names one of the twelve built-in scene systems, and the name is what
the loader resolves.

## The shapes that are not a roster count

The four quadrants are addressed in the same order on every backend, and the
four systems named in the cohort above are the ones a beginner meets first.

A capture writes through the file systems the host provides, which is why a
path that cannot be created is reported at the boundary rather than at the
first frame.

On 2026-09-14 the roster held fourteen systems. <!-- count-allow: a dated record inside a live document -->

The fourteen distinct built-in scene systems are three words apart from their
count, which this gate does not reach and says so.

## A command is not a claim

```sh
# twelve systems is prose inside a fence, and a fence is skipped
grep -rn "twelve systems" docs/
```
