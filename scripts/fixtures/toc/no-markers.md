# A document with no markers

## It has headings

Plenty of them, at several levels.

### And nested ones

But no `toc:begin` anywhere, so `scripts/toc.mjs` must leave every byte of it alone. A generator
that inserted a block into any document with headings would rewrite most of this repository.

## Another section
