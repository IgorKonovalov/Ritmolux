# Entrance B — seeded, and deliberately NOT in scope

This file is the boundary check. It is an Entrance B document, so it keeps its
bare citations and the gate must never look at it:

Measured at Plan 0044 Phase 2, the capture harness steps a fixed 1/60 s
(ADR-0045). The reachability walker reports a `COMP` line (ADR-0043), and
Plan 0048 Phase 7 retuned nine bindings against it.

If any of those four is ever reported, the scope list in the gate has widened
past ADR-0168's decision.
