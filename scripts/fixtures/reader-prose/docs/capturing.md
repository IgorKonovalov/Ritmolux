# Headless capture — seeded, and now IN scope

This file used to be the boundary check. The scope list has since widened to the
whole published set minus the install pages and the contributor group, so a
capture page is a reader page now and these four citations are breaks:

Measured at Plan 0044 Phase 2, the capture harness steps a fixed 1/60 s
(ADR-0045). The reachability walker reports a `COMP` line (ADR-0043), and
Plan 0048 Phase 7 retuned nine bindings against it.

If any of those four stops being reported, the scope list has narrowed back.
