# The palette surface — the seeded colour page

One bare citation, in the trailing-parenthetical shape a heading and a block end
both take: the backdrop samples the same baked LUT (Plan 0072).

The rest is clean prose. A stop is authored in sRGB and decoded once when the
LUT is baked, so a plateau written `#c81423` comes back `#c81423`.
