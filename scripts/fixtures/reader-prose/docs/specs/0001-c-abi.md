# Spec — C ABI — seeded clean, nested one directory deeper

The specs live under `docs/specs/`, so this file also checks that the gate reads
a path with a directory in it rather than assuming every reader document is a
direct child of `docs/`. Its citation is a link:
[ADR-0003](../../adrs/0003-a-fixture-target.md).
