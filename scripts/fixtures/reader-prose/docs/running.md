# Running the app — seeded clean, with a linked citation

An operator page carries the same rule as a preset page, and its one citation is
inside a link: [ADR-0169](../adrs/0169-a-fixture-target.md) decided the group this
page sits in. The gate must count that citation and convict none of it.
