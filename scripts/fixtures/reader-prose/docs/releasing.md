# Releasing — seeded, and deliberately NOT in scope

This file is the boundary check that `docs/capturing.md` used to be. It is a
**Contribute** page: a contributor reading about the release process wants the
working record, so it keeps its bare citations and the gate must never look at
it.

The cadence is one bump per plan close (ADR-0005), the tag job was reshaped at
Plan 0102, and Plan 0115 Phase 6 added the second artifact.

If any of those three is ever reported, the scope list has widened past
ADR-0168's decision.
