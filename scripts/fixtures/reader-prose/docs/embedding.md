# Embedding the core — seeded clean, and inline code is NOT a hole

An embedder's page. Its one citation is a link:
[ADR-0117](../adrs/0117-a-fixture-target.md) added the roster calls.

**A citation inside a code span is still convicted**, unlike one inside a fence.
That asymmetry is deliberate and easy to assume away, so this file is clean
precisely to record that no exemption was seeded for it.
