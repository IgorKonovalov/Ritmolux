{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Ritmolux preset",
  "description": "Generated from the engine's own parameter and table declarations. Do not edit by hand: re-run the core test suite with RLX_UPDATE_PRESET_SCHEMA=1.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "system": {
      "type": "string",
      "enum": ["fragment_field", "swarm", "parametric_curve", "lsystem", "star_pattern", "reaction_diffusion", "attractor", "spectrum", "emitter", "shape_field", "warp_mesh", "shape_collage", "analytic_field", "cellular"],
      "description": "Which built-in scene this preset drives. Required."
    },
    "name": {
      "type": "string",
      "description": "Human-readable name; absent means the system's own name."
    },
    "representative": {
      "type": "boolean",
      "description": "Whether this preset is one of its family's samples for the narrowed test tier.",
      "markdownDescription": "Whether this preset is one of its family's samples for the narrowed test tier.\n\nDefault `false`."
    },
    "params": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "One expression per named parameter; an unbound parameter keeps its default."
    },
    "per_vertex": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Bindings evaluated once per mesh vertex, with x/y/rad/ang in scope."
    },
    "smoothing": {
      "type": "object",
      "additionalProperties": {
        "anyOf": [
          { "type": "number" },
          { "type": "object", "additionalProperties": false, "properties": { "attack": { "type": "number" }, "release": { "type": "number" } } }
        ]
      },
      "description": "Per-parameter easing in seconds; an unlisted parameter is applied instantly."
    },
    "hold": {
      "type": "object",
      "additionalProperties": {
        "anyOf": [
          { "type": "number" },
          { "type": "string", "examples": ["beat", "bar"] }
        ]
      },
      "description": "Per-parameter sample-and-hold; an unlisted parameter is read every frame."
    },
    "latch": {
      "type": "object",
      "additionalProperties": {
        "$ref": "#/definitions/latch"
      },
      "description": "Named armed-and-fired events an expression can read as a variable."
    },
    "curve": {
      "$ref": "#/definitions/curve",
      "description": "The parametric curve's family."
    },
    "generator": {
      "$ref": "#/definitions/generator",
      "description": "The L-system's grammar or the star pattern's tiling, plus the preset's seed."
    },
    "particles": {
      "$ref": "#/definitions/particles",
      "description": "The attractor family, its density, and what morph travels towards."
    },
    "path": {
      "$ref": "#/definitions/path",
      "description": "An authored silhouette for the shape field, as inline SVG path data."
    },
    "spectrum": {
      "$ref": "#/definitions/spectrum",
      "description": "How the readout divides the frequency axis and what figure it forms."
    },
    "mesh": {
      "$ref": "#/definitions/mesh",
      "description": "The warp mesh's grid, in cells."
    },
    "field": {
      "$ref": "#/definitions/field",
      "description": "Which closed-form world the analytic field draws."
    },
    "cellular": {
      "$ref": "#/definitions/cellular",
      "description": "Which automaton the cellular system runs, and on how large a grid."
    },
    "milk": {
      "$ref": "#/definitions/milk",
      "description": "A converted MilkDrop preset's compiled programs and shaders."
    },
    "feedback": {
      "$ref": "#/definitions/feedback",
      "description": "Which warp the accumulation buffers resample their past through, and how this frame is deposited onto it."
    },
    "palette": {
      "$ref": "#/definitions/palette",
      "description": "The colour gradient; absent means the built-in spectrum."
    },
    "palette_b": {
      "$ref": "#/definitions/palette",
      "description": "The crossfade target a bindable palette_mix travels towards."
    },
    "occupancy": {
      "$ref": "#/definitions/occupancy",
      "description": "Parameters whose clamp bounds are meant to pin, exempted from the saturation gate."
    },
    "layer": {
      "$ref": "#/definitions/layer",
      "description": "A second scene drawn with the first."
    }
  },
  "allOf": [
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "fragment_field" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "warp": { "$ref": "#/definitions/param.warp.fragment_field" },
            "field_speed": { "$ref": "#/definitions/param.field_speed" },
            "fold_speed": { "$ref": "#/definitions/param.fold_speed" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "glow": { "$ref": "#/definitions/param.glow.fragment_field" },
            "flash": { "$ref": "#/definitions/param.flash" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "color_span": { "$ref": "#/definitions/param.color_span.fragment_field" },
            "color_center": { "$ref": "#/definitions/param.color_center.fragment_field" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "swarm" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "force": { "$ref": "#/definitions/param.force" },
            "spin": { "$ref": "#/definitions/param.spin.swarm" },
            "burst": { "$ref": "#/definitions/param.burst" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "brightness": { "$ref": "#/definitions/param.brightness.swarm" },
            "size": { "$ref": "#/definitions/param.size.swarm" },
            "field_freq": { "$ref": "#/definitions/param.field_freq" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.swarm" },
            "hue_center": { "$ref": "#/definitions/param.hue_center.swarm" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "twinkle": { "$ref": "#/definitions/param.twinkle.swarm" },
            "size_spread": { "$ref": "#/definitions/param.size_spread.swarm" },
            "reseed": { "$ref": "#/definitions/param.reseed.swarm" },
            "shape": { "$ref": "#/definitions/param.shape" },
            "points": { "$ref": "#/definitions/param.points" },
            "star_valley": { "$ref": "#/definitions/param.star_valley" },
            "star_curve": { "$ref": "#/definitions/param.star_curve" },
            "star_jitter": { "$ref": "#/definitions/param.star_jitter" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "parametric_curve" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "n": { "$ref": "#/definitions/param.n" },
            "d": { "$ref": "#/definitions/param.d.parametric_curve" },
            "phase": { "$ref": "#/definitions/param.phase" },
            "radial_offset": { "$ref": "#/definitions/param.radial_offset" },
            "pen": { "$ref": "#/definitions/param.pen" },
            "sym": { "$ref": "#/definitions/param.sym" },
            "sharpness": { "$ref": "#/definitions/param.sharpness" },
            "lobe": { "$ref": "#/definitions/param.lobe" },
            "decay": { "$ref": "#/definitions/param.decay.parametric_curve" },
            "samples": { "$ref": "#/definitions/param.samples" },
            "thickness": { "$ref": "#/definitions/param.thickness.parametric_curve" },
            "hue": { "$ref": "#/definitions/param.hue.parametric_curve" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.parametric_curve" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "spin": { "$ref": "#/definitions/param.spin.parametric_curve" },
            "scale": { "$ref": "#/definitions/param.scale.parametric_curve" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "glow": { "$ref": "#/definitions/param.glow.parametric_curve" },
            "softness": { "$ref": "#/definitions/param.softness" },
            "stroke_blend": { "$ref": "#/definitions/param.stroke_blend" },
            "draw_progress": { "$ref": "#/definitions/param.draw_progress" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "mirror_order": { "$ref": "#/definitions/param.mirror_order" },
            "mirror_reflect": { "$ref": "#/definitions/param.mirror_reflect" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "lsystem" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "visible_depth": { "$ref": "#/definitions/param.visible_depth" },
            "rotation": { "$ref": "#/definitions/param.rotation.lsystem" },
            "hue": { "$ref": "#/definitions/param.hue.lsystem" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.parametric_curve" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "draw_progress": { "$ref": "#/definitions/param.draw_progress" },
            "thickness": { "$ref": "#/definitions/param.thickness.lsystem" },
            "scale": { "$ref": "#/definitions/param.scale.lsystem" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "glow": { "$ref": "#/definitions/param.glow.parametric_curve" },
            "softness": { "$ref": "#/definitions/param.softness" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "stroke_blend": { "$ref": "#/definitions/param.stroke_blend" },
            "mirror_order": { "$ref": "#/definitions/param.mirror_order" },
            "mirror_reflect": { "$ref": "#/definitions/param.mirror_reflect" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "star_pattern" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "variant": { "$ref": "#/definitions/param.variant" },
            "rotation": { "$ref": "#/definitions/param.rotation.star_pattern" },
            "hue": { "$ref": "#/definitions/param.hue.star_pattern" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.parametric_curve" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "draw_progress": { "$ref": "#/definitions/param.draw_progress" },
            "thickness": { "$ref": "#/definitions/param.thickness.parametric_curve" },
            "scale": { "$ref": "#/definitions/param.scale.lsystem" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "glow": { "$ref": "#/definitions/param.glow.parametric_curve" },
            "softness": { "$ref": "#/definitions/param.softness" },
            "stroke_blend": { "$ref": "#/definitions/param.stroke_blend" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "mirror_order": { "$ref": "#/definitions/param.mirror_order" },
            "mirror_reflect": { "$ref": "#/definitions/param.mirror_reflect" },
            "ring_phase": { "$ref": "#/definitions/param.ring_phase" },
            "ring_spread": { "$ref": "#/definitions/param.ring_spread" },
            "ring_scale": { "$ref": "#/definitions/param.ring_scale" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "reaction_diffusion" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "feed": { "$ref": "#/definitions/param.feed" },
            "kill": { "$ref": "#/definitions/param.kill" },
            "flow": { "$ref": "#/definitions/param.flow" },
            "inject": { "$ref": "#/definitions/param.inject" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "contour": { "$ref": "#/definitions/param.contour" },
            "hatch": { "$ref": "#/definitions/param.hatch" },
            "glow": { "$ref": "#/definitions/param.glow.reaction_diffusion" },
            "color_span": { "$ref": "#/definitions/param.color_span.reaction_diffusion" },
            "color_center": { "$ref": "#/definitions/param.color_center.reaction_diffusion" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "attractor" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "a": { "$ref": "#/definitions/param.a" },
            "b": { "$ref": "#/definitions/param.b" },
            "c": { "$ref": "#/definitions/param.c" },
            "d": { "$ref": "#/definitions/param.d.attractor" },
            "tuple": { "$ref": "#/definitions/param.tuple" },
            "size": { "$ref": "#/definitions/param.size.attractor" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "fade": { "$ref": "#/definitions/param.fade" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.attractor" },
            "hue_center": { "$ref": "#/definitions/param.hue_center.attractor" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "reseed": { "$ref": "#/definitions/param.reseed.attractor" },
            "perspective": { "$ref": "#/definitions/param.perspective" },
            "depth_fade": { "$ref": "#/definitions/param.depth_fade" },
            "depth_hue": { "$ref": "#/definitions/param.depth_hue" },
            "spin": { "$ref": "#/definitions/param.spin.attractor" },
            "morph": { "$ref": "#/definitions/param.morph.attractor" },
            "curl": { "$ref": "#/definitions/param.curl" },
            "vigor": { "$ref": "#/definitions/param.vigor" },
            "lean": { "$ref": "#/definitions/param.lean" },
            "bias": { "$ref": "#/definitions/param.bias" },
            "map_tint": { "$ref": "#/definitions/param.map_tint" },
            "map_hue": { "$ref": "#/definitions/param.map_hue" },
            "root_tint": { "$ref": "#/definitions/param.root_tint" },
            "root_hue": { "$ref": "#/definitions/param.root_hue" },
            "emergence": { "$ref": "#/definitions/param.emergence" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.attractor" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.attractor" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.attractor" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.attractor" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.attractor" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.attractor" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.attractor" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "spectrum" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "base": { "$ref": "#/definitions/param.base" },
            "scale": { "$ref": "#/definitions/param.scale.spectrum" },
            "curve": { "$ref": "#/definitions/param.curve" },
            "radius": { "$ref": "#/definitions/param.radius.spectrum" },
            "span": { "$ref": "#/definitions/param.span" },
            "baseline": { "$ref": "#/definitions/param.baseline" },
            "rotation": { "$ref": "#/definitions/param.rotation.spectrum" },
            "thickness": { "$ref": "#/definitions/param.thickness.spectrum" },
            "hue": { "$ref": "#/definitions/param.hue.spectrum" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.parametric_curve" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "glow": { "$ref": "#/definitions/param.glow.parametric_curve" },
            "softness": { "$ref": "#/definitions/param.softness" },
            "stroke_blend": { "$ref": "#/definitions/param.stroke_blend" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "mirror_order": { "$ref": "#/definitions/param.mirror_order" },
            "mirror_reflect": { "$ref": "#/definitions/param.mirror_reflect" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "emitter" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "spawn_rate": { "$ref": "#/definitions/param.spawn_rate" },
            "gravity": { "$ref": "#/definitions/param.gravity" },
            "launch_speed": { "$ref": "#/definitions/param.launch_speed" },
            "launch_angle": { "$ref": "#/definitions/param.launch_angle" },
            "spread": { "$ref": "#/definitions/param.spread" },
            "lifetime": { "$ref": "#/definitions/param.lifetime" },
            "lifetime_spread": { "$ref": "#/definitions/param.lifetime_spread" },
            "source_y": { "$ref": "#/definitions/param.source_y" },
            "source_width": { "$ref": "#/definitions/param.source_width" },
            "spawn_fade": { "$ref": "#/definitions/param.spawn_fade" },
            "prewarm": { "$ref": "#/definitions/param.prewarm" },
            "size": { "$ref": "#/definitions/param.size.emitter" },
            "size_spread": { "$ref": "#/definitions/param.size_spread.emitter" },
            "spin": { "$ref": "#/definitions/param.spin.emitter" },
            "twinkle": { "$ref": "#/definitions/param.twinkle.emitter" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.emitter" },
            "hue_center": { "$ref": "#/definitions/param.hue_center.swarm" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "shape": { "$ref": "#/definitions/param.shape" },
            "points": { "$ref": "#/definitions/param.points" },
            "star_valley": { "$ref": "#/definitions/param.star_valley" },
            "star_curve": { "$ref": "#/definitions/param.star_curve" },
            "star_jitter": { "$ref": "#/definitions/param.star_jitter" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "shape_field" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "shape": { "$ref": "#/definitions/param.shape" },
            "points": { "$ref": "#/definitions/param.points" },
            "star_valley": { "$ref": "#/definitions/param.star_valley" },
            "star_curve": { "$ref": "#/definitions/param.star_curve" },
            "star_jitter": { "$ref": "#/definitions/param.star_jitter" },
            "scale": { "$ref": "#/definitions/param.scale.shape_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "color_span": { "$ref": "#/definitions/param.color_span.shape_field" },
            "color_center": { "$ref": "#/definitions/param.color_center.shape_field" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "gamma": { "$ref": "#/definitions/param.gamma.shape_field" },
            "coord_mode": { "$ref": "#/definitions/param.coord_mode" },
            "rotation": { "$ref": "#/definitions/param.rotation.shape_field" },
            "stroke": { "$ref": "#/definitions/param.stroke" },
            "morph": { "$ref": "#/definitions/param.morph.shape_field" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "warp_mesh" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "zoom": { "$ref": "#/definitions/param.zoom.warp_mesh" },
            "rot": { "$ref": "#/definitions/param.rot" },
            "cx": { "$ref": "#/definitions/param.cx" },
            "cy": { "$ref": "#/definitions/param.cy" },
            "dx": { "$ref": "#/definitions/param.dx" },
            "dy": { "$ref": "#/definitions/param.dy" },
            "sx": { "$ref": "#/definitions/param.sx" },
            "sy": { "$ref": "#/definitions/param.sy" },
            "warp": { "$ref": "#/definitions/param.warp.warp_mesh" },
            "warp_scale": { "$ref": "#/definitions/param.warp_scale" },
            "warp_speed": { "$ref": "#/definitions/param.warp_speed" },
            "decay": { "$ref": "#/definitions/param.decay.warp_mesh" },
            "deposit": { "$ref": "#/definitions/param.deposit" },
            "deposit_x": { "$ref": "#/definitions/param.deposit_x" },
            "deposit_y": { "$ref": "#/definitions/param.deposit_y" },
            "deposit_radius": { "$ref": "#/definitions/param.deposit_radius" },
            "deposit_width": { "$ref": "#/definitions/param.deposit_width" },
            "deposit_arms": { "$ref": "#/definitions/param.deposit_arms" },
            "deposit_twist": { "$ref": "#/definitions/param.deposit_twist" },
            "deposit_spin": { "$ref": "#/definitions/param.deposit_spin" },
            "gamma": { "$ref": "#/definitions/param.gamma.warp_mesh" },
            "wrap": { "$ref": "#/definitions/param.wrap" },
            "darken_center": { "$ref": "#/definitions/param.darken_center" },
            "brighten": { "$ref": "#/definitions/param.brighten" },
            "darken": { "$ref": "#/definitions/param.darken" },
            "solarize": { "$ref": "#/definitions/param.solarize" },
            "invert": { "$ref": "#/definitions/param.invert" },
            "echo_alpha": { "$ref": "#/definitions/param.echo_alpha" },
            "echo_zoom": { "$ref": "#/definitions/param.echo_zoom" },
            "echo_orient": { "$ref": "#/definitions/param.echo_orient" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "color_span": { "$ref": "#/definitions/param.color_span.warp_mesh" },
            "color_center": { "$ref": "#/definitions/param.color_center.shape_field" },
            "color_source": { "$ref": "#/definitions/param.color_source" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "shape_collage" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "count": { "$ref": "#/definitions/param.count" },
            "layout": { "$ref": "#/definitions/param.layout" },
            "seed": { "$ref": "#/definitions/param.seed" },
            "size_hierarchy": { "$ref": "#/definitions/param.size_hierarchy" },
            "angle_bias": { "$ref": "#/definitions/param.angle_bias" },
            "roster": { "$ref": "#/definitions/param.roster" },
            "density": { "$ref": "#/definitions/param.density" },
            "drift": { "$ref": "#/definitions/param.drift" },
            "spin": { "$ref": "#/definitions/param.spin.shape_collage" },
            "recompose": { "$ref": "#/definitions/param.recompose" },
            "recompose_blend": { "$ref": "#/definitions/param.recompose_blend" },
            "pump_size": { "$ref": "#/definitions/param.pump_size" },
            "pump_alpha": { "$ref": "#/definitions/param.pump_alpha" },
            "scale": { "$ref": "#/definitions/param.scale.shape_collage" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "paper": { "$ref": "#/definitions/param.paper" },
            "color_span": { "$ref": "#/definitions/param.color_span.shape_collage" },
            "palette_shift": { "$ref": "#/definitions/param.palette_shift" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "opacity": { "$ref": "#/definitions/param.opacity" },
            "edge_softness": { "$ref": "#/definitions/param.edge_softness" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "analytic_field" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "mode_n": { "$ref": "#/definitions/param.mode_n" },
            "mode_m": { "$ref": "#/definitions/param.mode_m" },
            "line_width": { "$ref": "#/definitions/param.line_width" },
            "plate_mix": { "$ref": "#/definitions/param.plate_mix" },
            "iterations": { "$ref": "#/definitions/param.iterations" },
            "c_re": { "$ref": "#/definitions/param.c_re" },
            "c_im": { "$ref": "#/definitions/param.c_im" },
            "escape_radius": { "$ref": "#/definitions/param.escape_radius" },
            "power": { "$ref": "#/definitions/param.power" },
            "interior": { "$ref": "#/definitions/param.interior" },
            "trap_radius": { "$ref": "#/definitions/param.trap_radius" },
            "trap_rotate": { "$ref": "#/definitions/param.trap_rotate" },
            "color_span": { "$ref": "#/definitions/param.color_span.analytic_field" },
            "color_center": { "$ref": "#/definitions/param.color_center.analytic_field" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["system"], "properties": { "system": { "const": "cellular" } } },
      "then": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "birth": { "$ref": "#/definitions/param.birth" },
            "survive": { "$ref": "#/definitions/param.survive" },
            "radius": { "$ref": "#/definitions/param.radius.cellular" },
            "birth_lo": { "$ref": "#/definitions/param.birth_lo" },
            "birth_hi": { "$ref": "#/definitions/param.birth_hi" },
            "survive_lo": { "$ref": "#/definitions/param.survive_lo" },
            "survive_hi": { "$ref": "#/definitions/param.survive_hi" },
            "states": { "$ref": "#/definitions/param.states" },
            "threshold": { "$ref": "#/definitions/param.threshold" },
            "step_rate": { "$ref": "#/definitions/param.step_rate" },
            "reseed": { "$ref": "#/definitions/param.reseed.cellular" },
            "trail": { "$ref": "#/definitions/param.trail" },
            "age_tint": { "$ref": "#/definitions/param.age_tint" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "bg_hue": { "$ref": "#/definitions/param.bg_hue" },
            "bg_bright": { "$ref": "#/definitions/param.bg_bright" },
            "bg_vignette": { "$ref": "#/definitions/param.bg_vignette" },
            "bg_angle": { "$ref": "#/definitions/param.bg_angle" },
            "bg_hue_span": { "$ref": "#/definitions/param.bg_hue_span" },
            "bg_shade": { "$ref": "#/definitions/param.bg_shade" },
            "bg_shade_end": { "$ref": "#/definitions/param.bg_shade_end" },
            "bg_ramp_gamma": { "$ref": "#/definitions/param.bg_ramp_gamma" },
            "bg_band_amount": { "$ref": "#/definitions/param.bg_band_amount" },
            "bg_band_angle": { "$ref": "#/definitions/param.bg_band_angle" },
            "bg_band_pos": { "$ref": "#/definitions/param.bg_band_pos" },
            "bg_band_width": { "$ref": "#/definitions/param.bg_band_width" },
            "bg_band_curve": { "$ref": "#/definitions/param.bg_band_curve" },
            "bg_band_hue": { "$ref": "#/definitions/param.bg_band_hue" },
            "bg_band_hue_span": { "$ref": "#/definitions/param.bg_band_hue_span" },
            "trails": { "$ref": "#/definitions/param.trails" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.trails" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.trails" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.trails" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.trails" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.trails" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.trails" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.trails" },
            "kaleido_order": { "$ref": "#/definitions/param.kaleido_order" },
            "kaleido_angle": { "$ref": "#/definitions/param.kaleido_angle" },
            "kaleido_center_x": { "$ref": "#/definitions/param.kaleido_center_x" },
            "kaleido_center_y": { "$ref": "#/definitions/param.kaleido_center_y" },
            "kaleido_edge": { "$ref": "#/definitions/param.kaleido_edge" },
            "kaleido_tile": { "$ref": "#/definitions/param.kaleido_tile" },
            "kaleido_radial": { "$ref": "#/definitions/param.kaleido_radial" },
            "kaleido_spiral": { "$ref": "#/definitions/param.kaleido_spiral" },
            "kaleido_zoom": { "$ref": "#/definitions/param.kaleido_zoom" },
            "kaleido_inner": { "$ref": "#/definitions/param.kaleido_inner" },
            "bloom_amount": { "$ref": "#/definitions/param.bloom_amount" },
            "bloom_threshold": { "$ref": "#/definitions/param.bloom_threshold" },
            "bloom_radius": { "$ref": "#/definitions/param.bloom_radius" },
            "occlude": { "$ref": "#/definitions/param.occlude" },
            "exposure": { "$ref": "#/definitions/param.exposure" },
            "ink_amount": { "$ref": "#/definitions/param.ink_amount" },
            "paper_hue": { "$ref": "#/definitions/param.paper_hue" },
            "paper_sat": { "$ref": "#/definitions/param.paper_sat" },
            "paper_bright": { "$ref": "#/definitions/param.paper_bright" },
            "ink_hue": { "$ref": "#/definitions/param.ink_hue" },
            "ink_sat": { "$ref": "#/definitions/param.ink_sat" },
            "ink_bright": { "$ref": "#/definitions/param.ink_bright" },
            "ink_gamma": { "$ref": "#/definitions/param.ink_gamma" }
          } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "fragment_field" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "warp": { "$ref": "#/definitions/param.warp.fragment_field" },
            "field_speed": { "$ref": "#/definitions/param.field_speed" },
            "fold_speed": { "$ref": "#/definitions/param.fold_speed" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "glow": { "$ref": "#/definitions/param.glow.fragment_field" },
            "flash": { "$ref": "#/definitions/param.flash" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "color_span": { "$ref": "#/definitions/param.color_span.fragment_field" },
            "color_center": { "$ref": "#/definitions/param.color_center.fragment_field" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "swarm" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "force": { "$ref": "#/definitions/param.force" },
            "spin": { "$ref": "#/definitions/param.spin.swarm" },
            "burst": { "$ref": "#/definitions/param.burst" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "brightness": { "$ref": "#/definitions/param.brightness.swarm" },
            "size": { "$ref": "#/definitions/param.size.swarm" },
            "field_freq": { "$ref": "#/definitions/param.field_freq" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.swarm" },
            "hue_center": { "$ref": "#/definitions/param.hue_center.swarm" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "twinkle": { "$ref": "#/definitions/param.twinkle.swarm" },
            "size_spread": { "$ref": "#/definitions/param.size_spread.swarm" },
            "reseed": { "$ref": "#/definitions/param.reseed.swarm" },
            "shape": { "$ref": "#/definitions/param.shape" },
            "points": { "$ref": "#/definitions/param.points" },
            "star_valley": { "$ref": "#/definitions/param.star_valley" },
            "star_curve": { "$ref": "#/definitions/param.star_curve" },
            "star_jitter": { "$ref": "#/definitions/param.star_jitter" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "parametric_curve" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "n": { "$ref": "#/definitions/param.n" },
            "d": { "$ref": "#/definitions/param.d.parametric_curve" },
            "phase": { "$ref": "#/definitions/param.phase" },
            "radial_offset": { "$ref": "#/definitions/param.radial_offset" },
            "pen": { "$ref": "#/definitions/param.pen" },
            "sym": { "$ref": "#/definitions/param.sym" },
            "sharpness": { "$ref": "#/definitions/param.sharpness" },
            "lobe": { "$ref": "#/definitions/param.lobe" },
            "decay": { "$ref": "#/definitions/param.decay.parametric_curve" },
            "samples": { "$ref": "#/definitions/param.samples" },
            "thickness": { "$ref": "#/definitions/param.thickness.parametric_curve" },
            "hue": { "$ref": "#/definitions/param.hue.parametric_curve" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.parametric_curve" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "spin": { "$ref": "#/definitions/param.spin.parametric_curve" },
            "scale": { "$ref": "#/definitions/param.scale.parametric_curve" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "glow": { "$ref": "#/definitions/param.glow.parametric_curve" },
            "softness": { "$ref": "#/definitions/param.softness" },
            "stroke_blend": { "$ref": "#/definitions/param.stroke_blend" },
            "draw_progress": { "$ref": "#/definitions/param.draw_progress" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "mirror_order": { "$ref": "#/definitions/param.mirror_order" },
            "mirror_reflect": { "$ref": "#/definitions/param.mirror_reflect" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "lsystem" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "visible_depth": { "$ref": "#/definitions/param.visible_depth" },
            "rotation": { "$ref": "#/definitions/param.rotation.lsystem" },
            "hue": { "$ref": "#/definitions/param.hue.lsystem" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.parametric_curve" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "draw_progress": { "$ref": "#/definitions/param.draw_progress" },
            "thickness": { "$ref": "#/definitions/param.thickness.lsystem" },
            "scale": { "$ref": "#/definitions/param.scale.lsystem" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "glow": { "$ref": "#/definitions/param.glow.parametric_curve" },
            "softness": { "$ref": "#/definitions/param.softness" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "stroke_blend": { "$ref": "#/definitions/param.stroke_blend" },
            "mirror_order": { "$ref": "#/definitions/param.mirror_order" },
            "mirror_reflect": { "$ref": "#/definitions/param.mirror_reflect" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "star_pattern" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "variant": { "$ref": "#/definitions/param.variant" },
            "rotation": { "$ref": "#/definitions/param.rotation.star_pattern" },
            "hue": { "$ref": "#/definitions/param.hue.star_pattern" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.parametric_curve" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "draw_progress": { "$ref": "#/definitions/param.draw_progress" },
            "thickness": { "$ref": "#/definitions/param.thickness.parametric_curve" },
            "scale": { "$ref": "#/definitions/param.scale.lsystem" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "glow": { "$ref": "#/definitions/param.glow.parametric_curve" },
            "softness": { "$ref": "#/definitions/param.softness" },
            "stroke_blend": { "$ref": "#/definitions/param.stroke_blend" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "mirror_order": { "$ref": "#/definitions/param.mirror_order" },
            "mirror_reflect": { "$ref": "#/definitions/param.mirror_reflect" },
            "ring_phase": { "$ref": "#/definitions/param.ring_phase" },
            "ring_spread": { "$ref": "#/definitions/param.ring_spread" },
            "ring_scale": { "$ref": "#/definitions/param.ring_scale" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "reaction_diffusion" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "feed": { "$ref": "#/definitions/param.feed" },
            "kill": { "$ref": "#/definitions/param.kill" },
            "flow": { "$ref": "#/definitions/param.flow" },
            "inject": { "$ref": "#/definitions/param.inject" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "contour": { "$ref": "#/definitions/param.contour" },
            "hatch": { "$ref": "#/definitions/param.hatch" },
            "glow": { "$ref": "#/definitions/param.glow.reaction_diffusion" },
            "color_span": { "$ref": "#/definitions/param.color_span.reaction_diffusion" },
            "color_center": { "$ref": "#/definitions/param.color_center.reaction_diffusion" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "attractor" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "a": { "$ref": "#/definitions/param.a" },
            "b": { "$ref": "#/definitions/param.b" },
            "c": { "$ref": "#/definitions/param.c" },
            "d": { "$ref": "#/definitions/param.d.attractor" },
            "tuple": { "$ref": "#/definitions/param.tuple" },
            "size": { "$ref": "#/definitions/param.size.attractor" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "fade": { "$ref": "#/definitions/param.fade" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.attractor" },
            "hue_center": { "$ref": "#/definitions/param.hue_center.attractor" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "reseed": { "$ref": "#/definitions/param.reseed.attractor" },
            "perspective": { "$ref": "#/definitions/param.perspective" },
            "depth_fade": { "$ref": "#/definitions/param.depth_fade" },
            "depth_hue": { "$ref": "#/definitions/param.depth_hue" },
            "spin": { "$ref": "#/definitions/param.spin.attractor" },
            "morph": { "$ref": "#/definitions/param.morph.attractor" },
            "curl": { "$ref": "#/definitions/param.curl" },
            "vigor": { "$ref": "#/definitions/param.vigor" },
            "lean": { "$ref": "#/definitions/param.lean" },
            "bias": { "$ref": "#/definitions/param.bias" },
            "map_tint": { "$ref": "#/definitions/param.map_tint" },
            "map_hue": { "$ref": "#/definitions/param.map_hue" },
            "root_tint": { "$ref": "#/definitions/param.root_tint" },
            "root_hue": { "$ref": "#/definitions/param.root_hue" },
            "emergence": { "$ref": "#/definitions/param.emergence" },
            "fb_zoom": { "$ref": "#/definitions/param.fb_zoom.attractor" },
            "fb_rotate": { "$ref": "#/definitions/param.fb_rotate.attractor" },
            "fb_dx": { "$ref": "#/definitions/param.fb_dx.attractor" },
            "fb_dy": { "$ref": "#/definitions/param.fb_dy.attractor" },
            "fb_center_x": { "$ref": "#/definitions/param.fb_center_x.attractor" },
            "fb_center_y": { "$ref": "#/definitions/param.fb_center_y.attractor" },
            "fb_warp": { "$ref": "#/definitions/param.fb_warp.attractor" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "spectrum" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "base": { "$ref": "#/definitions/param.base" },
            "scale": { "$ref": "#/definitions/param.scale.spectrum" },
            "curve": { "$ref": "#/definitions/param.curve" },
            "radius": { "$ref": "#/definitions/param.radius.spectrum" },
            "span": { "$ref": "#/definitions/param.span" },
            "baseline": { "$ref": "#/definitions/param.baseline" },
            "rotation": { "$ref": "#/definitions/param.rotation.spectrum" },
            "thickness": { "$ref": "#/definitions/param.thickness.spectrum" },
            "hue": { "$ref": "#/definitions/param.hue.spectrum" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.parametric_curve" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "glow": { "$ref": "#/definitions/param.glow.parametric_curve" },
            "softness": { "$ref": "#/definitions/param.softness" },
            "stroke_blend": { "$ref": "#/definitions/param.stroke_blend" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "mirror_order": { "$ref": "#/definitions/param.mirror_order" },
            "mirror_reflect": { "$ref": "#/definitions/param.mirror_reflect" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "emitter" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "spawn_rate": { "$ref": "#/definitions/param.spawn_rate" },
            "gravity": { "$ref": "#/definitions/param.gravity" },
            "launch_speed": { "$ref": "#/definitions/param.launch_speed" },
            "launch_angle": { "$ref": "#/definitions/param.launch_angle" },
            "spread": { "$ref": "#/definitions/param.spread" },
            "lifetime": { "$ref": "#/definitions/param.lifetime" },
            "lifetime_spread": { "$ref": "#/definitions/param.lifetime_spread" },
            "source_y": { "$ref": "#/definitions/param.source_y" },
            "source_width": { "$ref": "#/definitions/param.source_width" },
            "spawn_fade": { "$ref": "#/definitions/param.spawn_fade" },
            "prewarm": { "$ref": "#/definitions/param.prewarm" },
            "size": { "$ref": "#/definitions/param.size.emitter" },
            "size_spread": { "$ref": "#/definitions/param.size_spread.emitter" },
            "spin": { "$ref": "#/definitions/param.spin.emitter" },
            "twinkle": { "$ref": "#/definitions/param.twinkle.emitter" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "hue_spread": { "$ref": "#/definitions/param.hue_spread.emitter" },
            "hue_center": { "$ref": "#/definitions/param.hue_center.swarm" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "shape": { "$ref": "#/definitions/param.shape" },
            "points": { "$ref": "#/definitions/param.points" },
            "star_valley": { "$ref": "#/definitions/param.star_valley" },
            "star_curve": { "$ref": "#/definitions/param.star_curve" },
            "star_jitter": { "$ref": "#/definitions/param.star_jitter" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "shape_field" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "shape": { "$ref": "#/definitions/param.shape" },
            "points": { "$ref": "#/definitions/param.points" },
            "star_valley": { "$ref": "#/definitions/param.star_valley" },
            "star_curve": { "$ref": "#/definitions/param.star_curve" },
            "star_jitter": { "$ref": "#/definitions/param.star_jitter" },
            "scale": { "$ref": "#/definitions/param.scale.shape_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "color_span": { "$ref": "#/definitions/param.color_span.shape_field" },
            "color_center": { "$ref": "#/definitions/param.color_center.shape_field" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "gamma": { "$ref": "#/definitions/param.gamma.shape_field" },
            "coord_mode": { "$ref": "#/definitions/param.coord_mode" },
            "rotation": { "$ref": "#/definitions/param.rotation.shape_field" },
            "stroke": { "$ref": "#/definitions/param.stroke" },
            "morph": { "$ref": "#/definitions/param.morph.shape_field" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "warp_mesh" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "zoom": { "$ref": "#/definitions/param.zoom.warp_mesh" },
            "rot": { "$ref": "#/definitions/param.rot" },
            "cx": { "$ref": "#/definitions/param.cx" },
            "cy": { "$ref": "#/definitions/param.cy" },
            "dx": { "$ref": "#/definitions/param.dx" },
            "dy": { "$ref": "#/definitions/param.dy" },
            "sx": { "$ref": "#/definitions/param.sx" },
            "sy": { "$ref": "#/definitions/param.sy" },
            "warp": { "$ref": "#/definitions/param.warp.warp_mesh" },
            "warp_scale": { "$ref": "#/definitions/param.warp_scale" },
            "warp_speed": { "$ref": "#/definitions/param.warp_speed" },
            "decay": { "$ref": "#/definitions/param.decay.warp_mesh" },
            "deposit": { "$ref": "#/definitions/param.deposit" },
            "deposit_x": { "$ref": "#/definitions/param.deposit_x" },
            "deposit_y": { "$ref": "#/definitions/param.deposit_y" },
            "deposit_radius": { "$ref": "#/definitions/param.deposit_radius" },
            "deposit_width": { "$ref": "#/definitions/param.deposit_width" },
            "deposit_arms": { "$ref": "#/definitions/param.deposit_arms" },
            "deposit_twist": { "$ref": "#/definitions/param.deposit_twist" },
            "deposit_spin": { "$ref": "#/definitions/param.deposit_spin" },
            "gamma": { "$ref": "#/definitions/param.gamma.warp_mesh" },
            "wrap": { "$ref": "#/definitions/param.wrap" },
            "darken_center": { "$ref": "#/definitions/param.darken_center" },
            "brighten": { "$ref": "#/definitions/param.brighten" },
            "darken": { "$ref": "#/definitions/param.darken" },
            "solarize": { "$ref": "#/definitions/param.solarize" },
            "invert": { "$ref": "#/definitions/param.invert" },
            "echo_alpha": { "$ref": "#/definitions/param.echo_alpha" },
            "echo_zoom": { "$ref": "#/definitions/param.echo_zoom" },
            "echo_orient": { "$ref": "#/definitions/param.echo_orient" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "color_span": { "$ref": "#/definitions/param.color_span.warp_mesh" },
            "color_center": { "$ref": "#/definitions/param.color_center.shape_field" },
            "color_source": { "$ref": "#/definitions/param.color_source" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "shape_collage" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "count": { "$ref": "#/definitions/param.count" },
            "layout": { "$ref": "#/definitions/param.layout" },
            "seed": { "$ref": "#/definitions/param.seed" },
            "size_hierarchy": { "$ref": "#/definitions/param.size_hierarchy" },
            "angle_bias": { "$ref": "#/definitions/param.angle_bias" },
            "roster": { "$ref": "#/definitions/param.roster" },
            "density": { "$ref": "#/definitions/param.density" },
            "drift": { "$ref": "#/definitions/param.drift" },
            "spin": { "$ref": "#/definitions/param.spin.shape_collage" },
            "recompose": { "$ref": "#/definitions/param.recompose" },
            "recompose_blend": { "$ref": "#/definitions/param.recompose_blend" },
            "pump_size": { "$ref": "#/definitions/param.pump_size" },
            "pump_alpha": { "$ref": "#/definitions/param.pump_alpha" },
            "scale": { "$ref": "#/definitions/param.scale.shape_collage" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "paper": { "$ref": "#/definitions/param.paper" },
            "color_span": { "$ref": "#/definitions/param.color_span.shape_collage" },
            "palette_shift": { "$ref": "#/definitions/param.palette_shift" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "opacity": { "$ref": "#/definitions/param.opacity" },
            "edge_softness": { "$ref": "#/definitions/param.edge_softness" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "analytic_field" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "mode_n": { "$ref": "#/definitions/param.mode_n" },
            "mode_m": { "$ref": "#/definitions/param.mode_m" },
            "line_width": { "$ref": "#/definitions/param.line_width" },
            "plate_mix": { "$ref": "#/definitions/param.plate_mix" },
            "iterations": { "$ref": "#/definitions/param.iterations" },
            "c_re": { "$ref": "#/definitions/param.c_re" },
            "c_im": { "$ref": "#/definitions/param.c_im" },
            "escape_radius": { "$ref": "#/definitions/param.escape_radius" },
            "power": { "$ref": "#/definitions/param.power" },
            "interior": { "$ref": "#/definitions/param.interior" },
            "trap_radius": { "$ref": "#/definitions/param.trap_radius" },
            "trap_rotate": { "$ref": "#/definitions/param.trap_rotate" },
            "color_span": { "$ref": "#/definitions/param.color_span.analytic_field" },
            "color_center": { "$ref": "#/definitions/param.color_center.analytic_field" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" }
          } } } } } }
    },
    {
      "if": { "required": ["layer"], "properties": { "layer": { "required": ["system"], "properties": { "system": { "const": "cellular" } } } } },
      "then": { "properties": { "layer": { "properties": { "params": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "birth": { "$ref": "#/definitions/param.birth" },
            "survive": { "$ref": "#/definitions/param.survive" },
            "radius": { "$ref": "#/definitions/param.radius.cellular" },
            "birth_lo": { "$ref": "#/definitions/param.birth_lo" },
            "birth_hi": { "$ref": "#/definitions/param.birth_hi" },
            "survive_lo": { "$ref": "#/definitions/param.survive_lo" },
            "survive_hi": { "$ref": "#/definitions/param.survive_hi" },
            "states": { "$ref": "#/definitions/param.states" },
            "threshold": { "$ref": "#/definitions/param.threshold" },
            "step_rate": { "$ref": "#/definitions/param.step_rate" },
            "reseed": { "$ref": "#/definitions/param.reseed.cellular" },
            "trail": { "$ref": "#/definitions/param.trail" },
            "age_tint": { "$ref": "#/definitions/param.age_tint" },
            "brightness": { "$ref": "#/definitions/param.brightness.parametric_curve" },
            "hue": { "$ref": "#/definitions/param.hue.fragment_field" },
            "zoom": { "$ref": "#/definitions/param.zoom.fragment_field" },
            "pan_x": { "$ref": "#/definitions/param.pan_x" },
            "pan_y": { "$ref": "#/definitions/param.pan_y" },
            "saturation": { "$ref": "#/definitions/param.saturation" },
            "palette_mix": { "$ref": "#/definitions/param.palette_mix" },
            "palette_steps": { "$ref": "#/definitions/param.palette_steps" },
            "palette_contour": { "$ref": "#/definitions/param.palette_contour" },
            "palette_contour_style": { "$ref": "#/definitions/param.palette_contour_style" },
            "palette_contour_ink": { "$ref": "#/definitions/param.palette_contour_ink" }
          } } } } } }
    }
  ],
  "definitions": {
    "layer": {
      "description": "A second scene drawn with the first, with its own system, bindings and structure.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "system": {
          "type": "string",
          "enum": ["fragment_field", "swarm", "parametric_curve", "lsystem", "star_pattern", "reaction_diffusion", "attractor", "spectrum", "emitter", "shape_field", "warp_mesh", "shape_collage", "analytic_field", "cellular"],
          "description": "Which built-in scene the layer draws. Required."
        },
        "join": {
          "type": "string",
          "enum": ["under", "over"],
          "description": "Where the layer joins: into the main scene's target, or into the chain as its own image.",
          "markdownDescription": "Where the layer joins: into the main scene's target, or into the chain as its own image.\n\nDefault `under`."
        },
        "blend": {
          "type": "string",
          "enum": ["add", "screen", "multiply", "overlay"],
          "description": "How an over-joined layer blends into the chain. Inert on an under join.",
          "markdownDescription": "How an over-joined layer blends into the chain. Inert on an under join.\n\nDefault `screen`."
        },
        "mix": {
          "type": "string",
          "description": "The over junction's amount; absent means full strength."
        },
        "params": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "The layer's own parameter bindings, namespaced to its scene."
        },
        "per_vertex": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "The layer's per-vertex bindings, for a warp-mesh layer."
        },
        "smoothing": {
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              { "type": "number" },
              { "type": "object", "additionalProperties": false, "properties": { "attack": { "type": "number" }, "release": { "type": "number" } } }
            ]
          },
          "description": "Per-parameter easing for the layer's bindings."
        },
        "hold": {
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              { "type": "number" },
              { "type": "string", "examples": ["beat", "bar"] }
            ]
          },
          "description": "Per-parameter sample-and-hold for the layer's bindings."
        },
        "curve": {
          "$ref": "#/definitions/curve",
          "description": "The layer's parametric curve family."
        },
        "generator": {
          "$ref": "#/definitions/generator",
          "description": "The layer's L-system grammar or star tiling."
        },
        "particles": {
          "$ref": "#/definitions/particles",
          "description": "The layer's attractor family."
        },
        "path": {
          "$ref": "#/definitions/path",
          "description": "The layer's authored silhouette."
        },
        "spectrum": {
          "$ref": "#/definitions/spectrum",
          "description": "The layer's readout configuration."
        },
        "mesh": {
          "$ref": "#/definitions/mesh",
          "description": "The layer's warp-mesh grid."
        },
        "field": {
          "$ref": "#/definitions/field",
          "description": "The layer's analytic-field family."
        },
        "cellular": {
          "$ref": "#/definitions/cellular",
          "description": "The layer's automaton family and grid."
        }
      }
    },
    "latch": {
      "description": "A gate armed on one condition and fired by the first rising edge of another inside the arming window.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "arm": {
          "type": "string",
          "description": "While this holds above 0.5 the latch is armed; its fall re-arms it. Required."
        },
        "fire": {
          "type": "string",
          "description": "The rising edge that fires an armed latch. Required."
        },
        "hold": {
          "type": "number",
          "description": "How long the fired latch reads 1, in seconds. 0 is a single frame.",
          "markdownDescription": "How long the fired latch reads 1, in seconds. 0 is a single frame.\n\nDefault `0`."
        }
      }
    },
    "curve": {
      "description": "Which parametric family the curve scene samples.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "family": {
          "type": "string",
          "enum": ["maurer_rose", "lissajous", "hypotrochoid", "superformula", "harmonograph"],
          "description": "The curve family. Required."
        }
      }
    },
    "generator": {
      "description": "The L-system's grammar or the star pattern's tiling, plus the salt every system's hash() and noise() mix in.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "axiom": {
          "type": "string",
          "description": "L-system: the starting string. Required on an lsystem preset."
        },
        "rules": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "L-system: production rules, each key a single predecessor character."
        },
        "angle_deg": {
          "type": "number",
          "description": "L-system: turn angle for + and -, in degrees.",
          "markdownDescription": "L-system: turn angle for + and -, in degrees.\n\nDefault `25`."
        },
        "max_depth": {
          "type": "integer",
          "description": "L-system: how many iterations to expand and cache.",
          "markdownDescription": "L-system: how many iterations to expand and cache.\n\nDefault `4`."
        },
        "seed": {
          "anyOf": [
            { "type": "number" },
            { "type": "string", "enum": ["random"] }
          ],
          "description": "The salt hash() and noise() mix into their argument; \"random\" draws one per launch and pins to 0 on every capture path.",
          "markdownDescription": "The salt hash() and noise() mix into their argument; \"random\" draws one per launch and pins to 0 on every capture path.\n\nDefault `0`."
        },
        "tiling": {
          "type": "string",
          "enum": ["square", "4", "4.4.4.4", "hexagon", "6", "6.6.6", "octagon", "8", "4.8.8", "dodecagon", "12", "3.12.12", "none"],
          "description": "Star pattern: the regular tiling the rosette is built on, or \"none\" for the ring ornament alone. Required on a star_pattern preset."
        },
        "contact_angle_deg": {
          "type": "number",
          "description": "Star pattern: the contact angle the interlace is struck at, in degrees.",
          "markdownDescription": "Star pattern: the contact angle the interlace is struck at, in degrees.\n\nDefault `30`."
        },
        "rings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ring"
          },
          "description": "Star pattern: concentric rings of repeated motifs filling the interior."
        }
      }
    },
    "ring": {
      "description": "One ring of repeated motifs inside a star rosette.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "motif": {
          "type": "string",
          "enum": ["circle", "petal", "teardrop", "diamond", "arc", "trefoil", "chevron", "scallop"],
          "description": "Which motif to repeat. Required."
        },
        "count": {
          "type": "integer",
          "description": "Copies around the ring. Required."
        },
        "radius": {
          "type": "number",
          "description": "Distance from the frame centre to each copy's centre. Required."
        },
        "scale": {
          "type": "number",
          "description": "Motif size multiplier.",
          "markdownDescription": "Motif size multiplier.\n\nDefault `0.25`."
        },
        "phase": {
          "type": "number",
          "description": "Angular offset of copy 0, in radians.",
          "markdownDescription": "Angular offset of copy 0, in radians.\n\nDefault `0`."
        }
      }
    },
    "particles": {
      "description": "Which strange attractor the compute-particle scene iterates, how much of the tier's budget it draws, and what morph travels towards.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "family": {
          "type": "string",
          "enum": ["de_jong", "clifford", "thomas", "lorenz", "fern", "tree", "dragon", "sierpinski", "spiral"],
          "description": "The attractor family: one of the four maps, or an IFS figure. Required."
        },
        "density": {
          "type": "number",
          "description": "Fraction of the tier's particle budget actually drawn.",
          "markdownDescription": "Fraction of the tier's particle budget actually drawn.\n\nDefault `1`."
        },
        "morph_to": {
          "type": "string",
          "enum": ["fern", "tree", "dragon", "sierpinski", "spiral"],
          "description": "The IFS figure morph travels towards; absent pins the figure and makes morph inert. An error on a map family."
        },
        "tuple_from": {
          "type": "integer",
          "description": "Map families: the near end of the tuple path morph walks, as a roster index.",
          "markdownDescription": "Map families: the near end of the tuple path morph walks, as a roster index.\n\nDefault `0`."
        },
        "tuple_to": {
          "type": "integer",
          "description": "Map families: the far end, and the key that turns the walk on. An error on an IFS figure."
        }
      }
    },
    "path": {
      "description": "An authored silhouette for the shape field, as inline SVG path data.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "d": {
          "type": "string",
          "description": "The contour, as SVG path data. Required."
        },
        "morph_to": {
          "type": "string",
          "description": "A second silhouette morph travels towards; absent makes morph inert."
        },
        "samples": {
          "type": "integer",
          "description": "The arity both contours are resampled to, and what the per-pixel field costs O(N) in.",
          "markdownDescription": "The arity both contours are resampled to, and what the per-pixel field costs O(N) in.\n\nDefault `64`."
        }
      }
    },
    "spectrum": {
      "description": "How the readout divides the frequency axis, what figure the elements form, and how fast each element follows its band.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "elements": {
          "type": "integer",
          "description": "Element count. A readout finer than the band array is refused.",
          "markdownDescription": "Element count. A readout finer than the band array is refused.\n\nDefault `24`."
        },
        "layout": {
          "type": "string",
          "enum": ["bars", "polyline", "radial_ring"],
          "description": "Which figure the elements form.",
          "markdownDescription": "Which figure the elements form.\n\nDefault `bars`."
        },
        "smoothing": {
          "anyOf": [
            { "type": "number" },
            { "type": "object", "additionalProperties": false, "properties": { "attack": { "type": "number" }, "release": { "type": "number" } } }
          ],
          "description": "Per-element easing in seconds, in the [smoothing] table's own vocabulary.",
          "markdownDescription": "Per-element easing in seconds, in the [smoothing] table's own vocabulary.\n\nDefault `0`."
        }
      }
    },
    "mesh": {
      "description": "The warp mesh's grid, in cells. Clamped to the tier at both consumers rather than at load.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "x": {
          "type": "integer",
          "description": "Cells across.",
          "markdownDescription": "Cells across.\n\nDefault `32`."
        },
        "y": {
          "type": "integer",
          "description": "Cells down.",
          "markdownDescription": "Cells down.\n\nDefault `24`."
        }
      }
    },
    "field": {
      "description": "Which closed-form world the analytic field draws.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "family": {
          "type": "string",
          "enum": ["chladni", "escape_time"],
          "description": "The field family. Required inside the table; an absent table is chladni."
        },
        "map": {
          "type": "string",
          "enum": ["julia", "mandelbrot"],
          "description": "Escape time only: julia iterates from the pixel with c the constant, mandelbrot makes the pixel c.",
          "markdownDescription": "Escape time only: julia iterates from the pixel with c the constant, mandelbrot makes the pixel c.\n\nDefault `julia`."
        },
        "trap": {
          "type": "string",
          "enum": ["none", "point", "line", "cross", "circle"],
          "description": "Escape time only: colour each pixel by how near its orbit came to this shape instead of by how fast it escaped.",
          "markdownDescription": "Escape time only: colour each pixel by how near its orbit came to this shape instead of by how fast it escaped.\n\nDefault `none`."
        }
      }
    },
    "cellular": {
      "description": "Which automaton the cellular system runs, on how large a grid, and whether its edges wrap.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "family": {
          "type": "string",
          "enum": ["life_like", "larger_than_life", "cyclic"],
          "description": "The automaton family.",
          "markdownDescription": "The automaton family.\n\nDefault `life_like`."
        },
        "grid": {
          "type": "integer",
          "description": "Cells per side. A content value, not a resolution: doubling it halves how large every pattern looks.",
          "markdownDescription": "Cells per side. A content value, not a resolution: doubling it halves how large every pattern looks.\n\nDefault `256`."
        },
        "wrap": {
          "type": "boolean",
          "description": "Whether the grid is a torus; false makes every cell past the border dead.",
          "markdownDescription": "Whether the grid is a torus; false makes every cell past the border dead.\n\nDefault `true`."
        }
      }
    },
    "milk": {
      "description": "A converted MilkDrop preset's compiled programs, its two shaders, and the frame-level choices that ride with them. Produced by the converter, not hand-authored.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "per_frame_init": {
          "type": "string",
          "description": "The once-at-load program, as assembly text."
        },
        "per_frame": {
          "type": "string",
          "description": "The once-per-frame program, as assembly text."
        },
        "per_vertex": {
          "type": "string",
          "description": "The once-per-mesh-vertex program, as assembly text."
        },
        "waves": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/milk_element"
          },
          "description": "The custom waves, each with its own programs."
        },
        "shapes": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/milk_element"
          },
          "description": "The custom shapes, each with its own programs."
        },
        "warp_shader": {
          "type": "string",
          "description": "The emitted warp shader body."
        },
        "comp_shader": {
          "type": "string",
          "description": "The emitted composite shader body."
        },
        "blur_level": {
          "type": "integer",
          "description": "How many blur levels the shaders sample, 0 to 3.",
          "markdownDescription": "How many blur levels the shaders sample, 0 to 3.\n\nDefault `0`."
        },
        "quantize_steps": {
          "type": "number",
          "description": "The warp field's quantization, in steps."
        }
      }
    },
    "milk_element": {
      "description": "One custom wave or shape: its programs, how many instances it draws, and how it is stroked.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "init": {
          "type": "string",
          "description": "The once-at-load program, as assembly text."
        },
        "per_frame": {
          "type": "string",
          "description": "The once-per-frame program, as assembly text."
        },
        "per_point": {
          "type": "string",
          "description": "The once-per-point program, as assembly text."
        },
        "count": {
          "type": "integer",
          "description": "Points along the element.",
          "markdownDescription": "Points along the element.\n\nDefault `0`."
        },
        "instances": {
          "type": "integer",
          "description": "How many copies are drawn.",
          "markdownDescription": "How many copies are drawn.\n\nDefault `1`."
        },
        "use_dots": {
          "type": "boolean",
          "description": "Draw points rather than a connected line.",
          "markdownDescription": "Draw points rather than a connected line.\n\nDefault `false`."
        },
        "thick": {
          "type": "boolean",
          "description": "Stroke at the thick width.",
          "markdownDescription": "Stroke at the thick width.\n\nDefault `false`."
        },
        "additive": {
          "type": "boolean",
          "description": "Add into the frame rather than blending over it.",
          "markdownDescription": "Add into the frame rather than blending over it.\n\nDefault `false`."
        }
      }
    },
    "feedback": {
      "description": "Which curated warp the accumulation buffers resample their past through, and how this frame's light is deposited onto it.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "warp": {
          "type": "string",
          "enum": ["none", "swirl", "ripple", "fisheye"],
          "description": "The warp the accumulation reads its previous frame through.",
          "markdownDescription": "The warp the accumulation reads its previous frame through.\n\nDefault `none`."
        },
        "blend": {
          "type": "string",
          "enum": ["max", "add"],
          "description": "How this frame is deposited: bounded by the source maximum, or summed.",
          "markdownDescription": "How this frame is deposited: bounded by the source maximum, or summed.\n\nDefault `max`."
        }
      }
    },
    "occupancy": {
      "description": "Parameters whose clamp bounds are meant to pin, exempted from the saturation gate. Harness-only; nothing per-frame reads it.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "exempt": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Parameter names whose clamps may sit at their bound."
        }
      }
    },
    "palette": {
      "description": "The colour gradient: a built-in name, or explicit stops. The two are mutually exclusive.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "enum": ["spectrum", "ember", "ice", "mono", "aurora"],
          "description": "A built-in gradient.",
          "markdownDescription": "A built-in gradient.\n\nDefault `spectrum`."
        },
        "stops": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/stop"
          },
          "description": "An explicit gradient, as positioned colours."
        }
      }
    },
    "stop": {
      "description": "One gradient stop: where it sits, and what colour it is. Authored in sRGB.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "at": {
          "type": "number",
          "description": "Position along the gradient, 0 to 1. Required."
        },
        "color": {
          "anyOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "number" } }
          ],
          "description": "The colour, as #rrggbb or [r, g, b] in 0..1. Required."
        }
      }
    },
    "param.warp.fragment_field": {
      "type": "string",
      "description": "Amplitude of the domain fold; 0 flattens the field into plain bands.",
      "markdownDescription": "Amplitude of the domain fold; 0 flattens the field into plain bands.\n\n- default `0.4`\n- typical range `0.0` to `1.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.field_speed": {
      "type": "string",
      "description": "How fast the field itself drifts, as a multiple of its base rate.",
      "markdownDescription": "How fast the field itself drifts, as a multiple of its base rate.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fold_speed": {
      "type": "string",
      "description": "How fast the fold turns, independently of the field's own drift.",
      "markdownDescription": "How fast the fold turns, independently of the field's own drift.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue.fragment_field": {
      "type": "string",
      "description": "Where this scene reads from the palette, as a coordinate along it rather than a colour.",
      "markdownDescription": "Where this scene reads from the palette, as a coordinate along it rather than a colour.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.zoom.fragment_field": {
      "type": "string",
      "description": "Scales the whole scene about its centre; above 1 fills more of the frame.",
      "markdownDescription": "Scales the whole scene about its centre; above 1 fills more of the frame.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.glow.fragment_field": {
      "type": "string",
      "description": "Overall light the field emits, before the composite sees it.",
      "markdownDescription": "Overall light the field emits, before the composite sees it.\n\n- default `0.7`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.flash": {
      "type": "string",
      "description": "Lifts the whole field toward white, for a beat-driven blink.",
      "markdownDescription": "Lifts the whole field toward white, for a beat-driven blink.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.pan_x": {
      "type": "string",
      "description": "Slides the whole scene sideways, in the scene's own units rather than pixels.",
      "markdownDescription": "Slides the whole scene sideways, in the scene's own units rather than pixels.\n\n- default `0.0`\n- no typical range declared\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.pan_y": {
      "type": "string",
      "description": "Slides the whole scene vertically, in the scene's own units rather than pixels.",
      "markdownDescription": "Slides the whole scene vertically, in the scene's own units rather than pixels.\n\n- default `0.0`\n- no typical range declared\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_span.fragment_field": {
      "type": "string",
      "description": "How much of the palette the field's range covers; 0 is one flat colour.",
      "markdownDescription": "How much of the palette the field's range covers; 0 is one flat colour.\n\n- default `0.6`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_center.fragment_field": {
      "type": "string",
      "description": "Shifts which part of the field's range lands in the middle of the palette.",
      "markdownDescription": "Shifts which part of the field's range lands in the middle of the palette.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.saturation": {
      "type": "string",
      "description": "Pulls the scene's colour toward grey; 0 is fully desaturated, 1 is the palette's own.",
      "markdownDescription": "Pulls the scene's colour toward grey; 0 is fully desaturated, 1 is the palette's own.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.palette_mix": {
      "type": "string",
      "description": "Crossfades from the preset's palette to its second one; 0 is the first, 1 the second.",
      "markdownDescription": "Crossfades from the preset's palette to its second one; 0 is the first, 1 the second.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.palette_steps": {
      "type": "string",
      "description": "Quantizes the palette into this many flat bands; 0 leaves it continuous.",
      "markdownDescription": "Quantizes the palette into this many flat bands; 0 leaves it continuous.\n\n- default `0.0`\n- typical range `0.0` to `16.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.palette_contour": {
      "type": "string",
      "description": "Draws a line at each band edge when the palette is stepped; 0 draws none.",
      "markdownDescription": "Draws a line at each band edge when the palette is stepped; 0 draws none.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.palette_contour_style": {
      "type": "string",
      "description": "Which line the contour draws: 0 a soft darkening, 1 a hard one, 2 a soft ink, 3 a hard ink.",
      "markdownDescription": "Which line the contour draws: 0 a soft darkening, 1 a hard one, 2 a soft ink, 3 a hard ink.\n\n- default `0.0`\n- typical range `0.0` to `3.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.palette_contour_ink": {
      "type": "string",
      "description": "Where along the palette an ink contour takes its colour; unread by the two black styles.",
      "markdownDescription": "Where along the palette an ink contour takes its colour; unread by the two black styles.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.force": {
      "type": "string",
      "description": "How hard the flow field pushes each particle, so higher is faster and straighter.",
      "markdownDescription": "How hard the flow field pushes each particle, so higher is faster and straighter.\n\n- default `1.4`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.spin.swarm": {
      "type": "string",
      "description": "Rotational bias added to the flow, curling the paths into vortices.",
      "markdownDescription": "Rotational bias added to the flow, curling the paths into vortices.\n\n- default `0.3`\n- typical range `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.burst": {
      "type": "string",
      "description": "An outward impulse from the centre, for a beat to throw the swarm apart.",
      "markdownDescription": "An outward impulse from the centre, for a beat to throw the swarm apart.\n\n- default `0.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.brightness.swarm": {
      "type": "string",
      "description": "The scene's overall light level, multiplying what it draws before the composite.",
      "markdownDescription": "The scene's overall light level, multiplying what it draws before the composite.\n\n- default `0.8`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.size.swarm": {
      "type": "string",
      "description": "Size of each particle's mark.",
      "markdownDescription": "Size of each particle's mark.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.field_freq": {
      "type": "string",
      "description": "Spatial frequency of the flow field; higher makes smaller, busier eddies.",
      "markdownDescription": "Spatial frequency of the flow field; higher makes smaller, busier eddies.\n\n- default `2.3`\n- typical range `0.5` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue_spread.swarm": {
      "type": "string",
      "description": "How far across the palette the particle band reaches.",
      "markdownDescription": "How far across the palette the particle band reaches.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue_center.swarm": {
      "type": "string",
      "description": "Where that band sits along the palette.",
      "markdownDescription": "Where that band sits along the palette.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.twinkle.swarm": {
      "type": "string",
      "description": "Per-particle brightness flicker, seeded so it is reproducible.",
      "markdownDescription": "Per-particle brightness flicker, seeded so it is reproducible.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.size_spread.swarm": {
      "type": "string",
      "description": "How much particle sizes vary about `size`; 0 makes them uniform.",
      "markdownDescription": "How much particle sizes vary about `size`; 0 makes them uniform.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.reseed.swarm": {
      "type": "string",
      "description": "Crossing zero throws every particle back to a fresh start position.",
      "markdownDescription": "Crossing zero throws every particle back to a fresh start position.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.shape": {
      "type": "string",
      "description": "Which silhouette each mark is drawn as - a disc, a square, a star, and so on.",
      "markdownDescription": "Which silhouette each mark is drawn as - a disc, a square, a star, and so on.\n\n- default `0.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.points": {
      "type": "string",
      "description": "How many points or sides the silhouette has, where the shape has a count at all.",
      "markdownDescription": "How many points or sides the silhouette has, where the shape has a count at all.\n\n- default `5.0`\n- typical range `3.0` to `16.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.star_valley": {
      "type": "string",
      "description": "How deep the notches between a star's points cut; near 1 the star becomes a disc.",
      "markdownDescription": "How deep the notches between a star's points cut; near 1 the star becomes a disc.\n\n- default `0.45`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.star_curve": {
      "type": "string",
      "description": "Bows a star's edges inward or outward instead of leaving them straight.",
      "markdownDescription": "Bows a star's edges inward or outward instead of leaving them straight.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.star_jitter": {
      "type": "string",
      "description": "Randomises each point's length by a seeded amount, so the star reads as hand-drawn.",
      "markdownDescription": "Randomises each point's length by a seeded amount, so the star reads as hand-drawn.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.n": {
      "type": "string",
      "description": "The figure's first number, read as a real value per family: the rose's petal number, the Lissajous and harmonograph x frequency, the hypotrochoid's signed radius ratio.",
      "markdownDescription": "The figure's first number, read as a real value per family: the rose's petal number, the Lissajous and harmonograph x frequency, the hypotrochoid's signed radius ratio.\n\n- default `6.0`\n- typical range depends on the family: `maurer_rose` `1.0` to `24.0`, `lissajous` `1.0` to `12.0`, `hypotrochoid` `-8.0` to `8.0`, `harmonograph` `1.0` to `12.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `superformula` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.d.parametric_curve": {
      "type": "string",
      "description": "The figure's second number, per family: the rose's sampling step in degrees, the Lissajous and harmonograph y frequency, the hypotrochoid's cusp count, the superformula's lobe skew.",
      "markdownDescription": "The figure's second number, per family: the rose's sampling step in degrees, the Lissajous and harmonograph y frequency, the hypotrochoid's cusp count, the superformula's lobe skew.\n\n- default `71.0`\n- typical range depends on the family: `maurer_rose` `1.0` to `360.0`, `lissajous` `1.0` to `12.0`, `hypotrochoid` `1.0` to `24.0`, `superformula` `0.25` to `4.0`, `harmonograph` `1.0` to `12.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.phase": {
      "type": "string",
      "description": "Offsets where the figure starts: inside the rose's sine, between the Lissajous and harmonograph axes, and at the hypotrochoid's pen.",
      "markdownDescription": "Offsets where the figure starts: inside the rose's sine, between the Lissajous and harmonograph axes, and at the hypotrochoid's pen.\n\n- default `0.0`\n- typical range depends on the family: `maurer_rose` `0.0` to `1.0`, `lissajous` `0.0` to `1.0`, `hypotrochoid` `0.0` to `1.0`, `harmonograph` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `superformula` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.radial_offset": {
      "type": "string",
      "description": "Pushes every point out from the centre, opening the figure into a ring.",
      "markdownDescription": "Pushes every point out from the centre, opening the figure into a ring.\n\n- default `0.0`\n- typical range depends on the family: `maurer_rose` `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `lissajous`, `hypotrochoid`, `superformula`, `harmonograph` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.pen": {
      "type": "string",
      "description": "How far the tracing point sits from the rolling circle's centre, in rolling radii: 1 draws cusps, less rounds them off, more throws them into loops.",
      "markdownDescription": "How far the tracing point sits from the rolling circle's centre, in rolling radii: 1 draws cusps, less rounds them off, more throws them into loops.\n\n- default `1.0`\n- typical range depends on the family: `hypotrochoid` `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `maurer_rose`, `lissajous`, `superformula`, `harmonograph` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.sym": {
      "type": "string",
      "description": "How many lobes the figure repeats around its centre, as a whole number.",
      "markdownDescription": "How many lobes the figure repeats around its centre, as a whole number.\n\n- default `5.0`\n- typical range depends on the family: `superformula` `1.0` to `24.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `maurer_rose`, `lissajous`, `hypotrochoid`, `harmonograph` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.sharpness": {
      "type": "string",
      "description": "How pointed the lobes are: low draws a spiky star, high rounds the figure toward a circle.",
      "markdownDescription": "How pointed the lobes are: low draws a spiky star, high rounds the figure toward a circle.\n\n- default `1.0`\n- typical range depends on the family: `superformula` `0.1` to `20.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `maurer_rose`, `lissajous`, `hypotrochoid`, `harmonograph` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.lobe": {
      "type": "string",
      "description": "How the lobes swell between their tips: low pinches them thin, high fills them into a polygon.",
      "markdownDescription": "How the lobes swell between their tips: low pinches them thin, high fills them into a polygon.\n\n- default `1.0`\n- typical range depends on the family: `superformula` `0.1` to `10.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `maurer_rose`, `lissajous`, `hypotrochoid`, `harmonograph` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.decay.parametric_curve": {
      "type": "string",
      "description": "How fast the pendulums die away along the trace: 0 closes the figure, more spirals it inward.",
      "markdownDescription": "How fast the pendulums die away along the trace: 0 closes the figure, more spirals it inward.\n\n- default `0.1`\n- typical range depends on the family: `harmonograph` `0.0` to `0.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `maurer_rose`, `lissajous`, `hypotrochoid`, `superformula` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.samples": {
      "type": "string",
      "description": "How many points the curve is drawn from; fewer reads as a polygon. Truncated, so a rise adds its next point on arrival.",
      "markdownDescription": "How many points the curve is drawn from; fewer reads as a polygon. Truncated, so a rise adds its next point on arrival.\n\n- default `361.0`\n- typical range `16.0` to `2048.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.thickness.parametric_curve": {
      "type": "string",
      "description": "Stroke width in pixels at the render target, before softness widens the falloff.",
      "markdownDescription": "Stroke width in pixels at the render target, before softness widens the falloff.\n\n- default `2.0`\n- typical range `0.5` to `12.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue.parametric_curve": {
      "type": "string",
      "description": "Where this scene reads from the palette, as a coordinate along it rather than a colour.",
      "markdownDescription": "Where this scene reads from the palette, as a coordinate along it rather than a colour.\n\n- default `0.6`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue_spread.parametric_curve": {
      "type": "string",
      "description": "How far along the palette the colour travels from one end of the figure to the other.",
      "markdownDescription": "How far along the palette the colour travels from one end of the figure to the other.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.spin.parametric_curve": {
      "type": "string",
      "description": "Turns per second the whole figure rotates by.",
      "markdownDescription": "Turns per second the whole figure rotates by.\n\n- default `0.1`\n- typical range `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.scale.parametric_curve": {
      "type": "string",
      "description": "Size of the figure within the frame, before the shared zoom is applied.",
      "markdownDescription": "Size of the figure within the frame, before the shared zoom is applied.\n\n- default `0.9`\n- typical range `0.1` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.brightness.parametric_curve": {
      "type": "string",
      "description": "The scene's overall light level, multiplying what it draws before the composite.",
      "markdownDescription": "The scene's overall light level, multiplying what it draws before the composite.\n\n- default `1.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.glow.parametric_curve": {
      "type": "string",
      "description": "Brightness of the halo around each stroke, on top of the stroke itself.",
      "markdownDescription": "Brightness of the halo around each stroke, on top of the stroke itself.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.softness": {
      "type": "string",
      "description": "How far a stroke's edge fades out; 0 is a hard line, 1 a wide glow with no core.",
      "markdownDescription": "How far a stroke's edge fades out; 0 is a hard line, 1 a wide glow with no core.\n\n- default `0.25`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.stroke_blend": {
      "type": "string",
      "description": "Moves the stroke from additive light toward opaque paint, so crossings stop brightening.",
      "markdownDescription": "Moves the stroke from additive light toward opaque paint, so crossings stop brightening.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.draw_progress": {
      "type": "string",
      "description": "How much of the figure is drawn, from its start; below 1 the line is still arriving.",
      "markdownDescription": "How much of the figure is drawn, from its start; below 1 the line is still arriving.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.mirror_order": {
      "type": "string",
      "description": "Repeats the geometry this many times around the centre; 1 draws it once.",
      "markdownDescription": "Repeats the geometry this many times around the centre; 1 draws it once.\n\n- default `1.0`\n- typical range `1.0` to `12.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.mirror_reflect": {
      "type": "string",
      "description": "Alternates the repeats into mirror images rather than plain rotations.",
      "markdownDescription": "Alternates the repeats into mirror images rather than plain rotations.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.visible_depth": {
      "type": "string",
      "description": "Which recursion generation is drawn, counted from 1 and capped at `max_depth`; a fraction floors to the generation below it, and anything under 2 draws the first.",
      "markdownDescription": "Which recursion generation is drawn, counted from 1 and capped at `max_depth`; a fraction floors to the generation below it, and anything under 2 draws the first.\n\n- default `1.0`\n- typical range `1.0` to `7.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.rotation.lsystem": {
      "type": "string",
      "description": "Turns the whole figure, in radians.",
      "markdownDescription": "Turns the whole figure, in radians.\n\n- default `0.0`\n- typical range `0.0` to `6.2831855` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue.lsystem": {
      "type": "string",
      "description": "Where this scene reads from the palette, as a coordinate along it rather than a colour.",
      "markdownDescription": "Where this scene reads from the palette, as a coordinate along it rather than a colour.\n\n- default `0.3`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.thickness.lsystem": {
      "type": "string",
      "description": "Stroke width in pixels at the render target, before softness widens the falloff.",
      "markdownDescription": "Stroke width in pixels at the render target, before softness widens the falloff.\n\n- default `1.8`\n- typical range `0.5` to `12.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.scale.lsystem": {
      "type": "string",
      "description": "Size of the figure within the frame, before the shared zoom is applied.",
      "markdownDescription": "Size of the figure within the frame, before the shared zoom is applied.\n\n- default `1.0`\n- typical range `0.1` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.variant": {
      "type": "string",
      "description": "Moves the construction's contact angle, continuously: this is an angle offset rather than an index into a list.",
      "markdownDescription": "Moves the construction's contact angle, continuously: this is an angle offset rather than an index into a list.\n\n- default `1.0`\n- typical range `0.0` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.rotation.star_pattern": {
      "type": "string",
      "description": "Turns the whole pattern, in radians.",
      "markdownDescription": "Turns the whole pattern, in radians.\n\n- default `0.0`\n- typical range `0.0` to `6.2831855` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue.star_pattern": {
      "type": "string",
      "description": "Where this scene reads from the palette, as a coordinate along it rather than a colour.",
      "markdownDescription": "Where this scene reads from the palette, as a coordinate along it rather than a colour.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.ring_phase": {
      "type": "string",
      "description": "Rotates each concentric ring against its neighbour.",
      "markdownDescription": "Rotates each concentric ring against its neighbour.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.ring_spread": {
      "type": "string",
      "description": "How far apart the rings sit radially.",
      "markdownDescription": "How far apart the rings sit radially.\n\n- default `1.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.ring_scale": {
      "type": "string",
      "description": "How much each ring grows over the one inside it.",
      "markdownDescription": "How much each ring grows over the one inside it.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.feed": {
      "type": "string",
      "description": "Feed rate of the reaction - with `kill`, it is what decides whether you get spots, stripes or mitosis.",
      "markdownDescription": "Feed rate of the reaction - with `kill`, it is what decides whether you get spots, stripes or mitosis.\n\n- default `0.0367`\n- typical range `0.01` to `0.09` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kill": {
      "type": "string",
      "description": "Kill rate of the reaction; small moves here change the pattern's whole character.",
      "markdownDescription": "Kill rate of the reaction; small moves here change the pattern's whole character.\n\n- default `0.0649`\n- typical range `0.03` to `0.07` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.flow": {
      "type": "string",
      "description": "How fast the simulation advances per second.",
      "markdownDescription": "How fast the simulation advances per second.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.inject": {
      "type": "string",
      "description": "Drops fresh reagent into the field, which is how a beat seeds new growth.",
      "markdownDescription": "Drops fresh reagent into the field, which is how a beat seeds new growth.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.contour": {
      "type": "string",
      "description": "How many bands the concentration is drawn as, as a real density: a fraction slides the whole set of iso-lines. 0 is a smooth gradient.",
      "markdownDescription": "How many bands the concentration is drawn as, as a real density: a fraction slides the whole set of iso-lines. 0 is a smooth gradient.\n\n- default `6.0`\n- typical range `0.0` to `24.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hatch": {
      "type": "string",
      "description": "Density of the hatching drawn along the concentration gradient.",
      "markdownDescription": "Density of the hatching drawn along the concentration gradient.\n\n- default `5.0`\n- typical range `0.0` to `24.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.glow.reaction_diffusion": {
      "type": "string",
      "description": "Overall light the field emits.",
      "markdownDescription": "Overall light the field emits.\n\n- default `1.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_span.reaction_diffusion": {
      "type": "string",
      "description": "How much of the palette the concentration range covers.",
      "markdownDescription": "How much of the palette the concentration range covers.\n\n- default `0.85`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_center.reaction_diffusion": {
      "type": "string",
      "description": "Shifts which concentration lands in the middle of the palette.",
      "markdownDescription": "Shifts which concentration lands in the middle of the palette.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.a": {
      "type": "string",
      "description": "First of the four family coefficients; what it means depends on the attractor family the tuple picked.",
      "markdownDescription": "First of the four family coefficients; what it means depends on the attractor family the tuple picked.\n\n- default `0.0`\n- typical range depends on the family: `de_jong` `-3.0` to `3.0`, `clifford` `-2.0` to `2.0`, `thomas` `0.0` to `0.25`, `lorenz` `5.0` to `20.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `fern`, `tree`, `dragon`, `sierpinski`, `spiral` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.b": {
      "type": "string",
      "description": "Second family coefficient - see the roster's attractor essay for what each family does with it.",
      "markdownDescription": "Second family coefficient - see the roster's attractor essay for what each family does with it.\n\n- default `0.0`\n- typical range depends on the family: `de_jong` `-3.0` to `3.0`, `clifford` `-2.0` to `2.0`, `lorenz` `20.0` to `130.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `thomas`, `fern`, `tree`, `dragon`, `sierpinski`, `spiral` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.c": {
      "type": "string",
      "description": "Third family coefficient, and on the IFS figures it means nothing at all.",
      "markdownDescription": "Third family coefficient, and on the IFS figures it means nothing at all.\n\n- default `0.0`\n- typical range depends on the family: `de_jong` `-3.0` to `3.0`, `clifford` `-2.0` to `2.0`, `lorenz` `0.5` to `4.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `thomas`, `fern`, `tree`, `dragon`, `sierpinski`, `spiral` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.d.attractor": {
      "type": "string",
      "description": "Fourth family coefficient; like the other three it is inert on the IFS figures.",
      "markdownDescription": "Fourth family coefficient; like the other three it is inert on the IFS figures.\n\n- default `0.0`\n- typical range depends on the family: `de_jong` `-3.0` to `3.0`, `clifford` `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `thomas`, `lorenz`, `fern`, `tree`, `dragon`, `sierpinski`, `spiral` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.tuple": {
      "type": "string",
      "description": "Picks a whole known-good figure - family, coefficients and framing together.",
      "markdownDescription": "Picks a whole known-good figure - family, coefficients and framing together.\n\n- default `0.0`\n- no typical range declared\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.size.attractor": {
      "type": "string",
      "description": "Size of each particle's deposit into the accumulation.",
      "markdownDescription": "Size of each particle's deposit into the accumulation.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fade": {
      "type": "string",
      "description": "How much of the accumulation survives each second; near 1 the figure builds up for a long time.",
      "markdownDescription": "How much of the accumulation survives each second; near 1 the figure builds up for a long time.\n\n- default `0.94`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue_spread.attractor": {
      "type": "string",
      "description": "How far across the palette the particle band reaches.",
      "markdownDescription": "How far across the palette the particle band reaches.\n\n- default `0.15`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue_center.attractor": {
      "type": "string",
      "description": "Where that band sits along the palette.",
      "markdownDescription": "Where that band sits along the palette.\n\n- default `0.075`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.reseed.attractor": {
      "type": "string",
      "description": "Crossing zero throws every particle back onto a fresh start position.",
      "markdownDescription": "Crossing zero throws every particle back onto a fresh start position.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.perspective": {
      "type": "string",
      "description": "How strongly depth shrinks a particle, turning a flat figure into a solid one.",
      "markdownDescription": "How strongly depth shrinks a particle, turning a flat figure into a solid one.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.depth_fade": {
      "type": "string",
      "description": "How much depth dims a particle, which is what reads as air between the layers.",
      "markdownDescription": "How much depth dims a particle, which is what reads as air between the layers.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.depth_hue": {
      "type": "string",
      "description": "Shifts colour with depth, so far parts of the figure sit elsewhere on the palette.",
      "markdownDescription": "Shifts colour with depth, so far parts of the figure sit elsewhere on the palette.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.spin.attractor": {
      "type": "string",
      "description": "Turns per second the figure rotates by about its vertical axis.",
      "markdownDescription": "Turns per second the figure rotates by about its vertical axis.\n\n- default `0.0`\n- typical range `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.morph.attractor": {
      "type": "string",
      "description": "Travels between the tuple's figure and the next one; the visible rate is steepest near zero.",
      "markdownDescription": "Travels between the tuple's figure and the next one; the visible rate is steepest near zero.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.curl": {
      "type": "string",
      "description": "Adds a rotational term to the map, curling the trajectories.",
      "markdownDescription": "Adds a rotational term to the map, curling the trajectories.\n\n- default `0.0`\n- typical range `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.vigor": {
      "type": "string",
      "description": "How far a particle moves per step, so higher spreads the figure and thins it.",
      "markdownDescription": "How far a particle moves per step, so higher spreads the figure and thins it.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.lean": {
      "type": "string",
      "description": "Tilts the map, breaking the figure's symmetry.",
      "markdownDescription": "Tilts the map, breaking the figure's symmetry.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bias": {
      "type": "string",
      "description": "Offsets the map, sliding the figure within its own attractor.",
      "markdownDescription": "Offsets the map, sliding the figure within its own attractor.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.map_tint": {
      "type": "string",
      "description": "How much a particle's colour follows which branch of the map produced it.",
      "markdownDescription": "How much a particle's colour follows which branch of the map produced it.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.map_hue": {
      "type": "string",
      "description": "How far apart on the palette those branches are placed.",
      "markdownDescription": "How far apart on the palette those branches are placed.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.root_tint": {
      "type": "string",
      "description": "How much a particle's colour follows the seed it started from.",
      "markdownDescription": "How much a particle's colour follows the seed it started from.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.root_hue": {
      "type": "string",
      "description": "How far apart on the palette those seeds are placed.",
      "markdownDescription": "How far apart on the palette those seeds are placed.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.emergence": {
      "type": "string",
      "description": "How many seconds the figure takes to settle out of its starting cloud.",
      "markdownDescription": "How many seconds the figure takes to settle out of its starting cloud.\n\n- default `8.0`\n- typical range `0.0` to `60.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_zoom.attractor": {
      "type": "string",
      "description": "Scale the attractor's own accumulation is grown by each second.",
      "markdownDescription": "Scale the attractor's own accumulation is grown by each second.\n\n- default `1.0`\n- typical range `0.9` to `1.1` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_rotate.attractor": {
      "type": "string",
      "description": "Turns per second that accumulation is rotated by.",
      "markdownDescription": "Turns per second that accumulation is rotated by.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_dx.attractor": {
      "type": "string",
      "description": "Sideways drift of that accumulation, in frame widths per second.",
      "markdownDescription": "Sideways drift of that accumulation, in frame widths per second.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_dy.attractor": {
      "type": "string",
      "description": "Vertical drift of that accumulation, in frame heights per second.",
      "markdownDescription": "Vertical drift of that accumulation, in frame heights per second.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_center_x.attractor": {
      "type": "string",
      "description": "The horizontal point its zoom and rotation pivot about, in uv.",
      "markdownDescription": "The horizontal point its zoom and rotation pivot about, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_center_y.attractor": {
      "type": "string",
      "description": "The vertical point its zoom and rotation pivot about, in uv.",
      "markdownDescription": "The vertical point its zoom and rotation pivot about, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_warp.attractor": {
      "type": "string",
      "description": "Amplitude of a swirl added to its feedback sample, so the trail curls.",
      "markdownDescription": "Amplitude of a swirl added to its feedback sample, so the trail curls.\n\n- default `0.0`\n- typical range `0.0` to `0.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.base": {
      "type": "string",
      "description": "Height the readout sits at when the band is silent.",
      "markdownDescription": "Height the readout sits at when the band is silent.\n\n- default `0.06`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.scale.spectrum": {
      "type": "string",
      "description": "How far a full band pushes the readout above its base.",
      "markdownDescription": "How far a full band pushes the readout above its base.\n\n- default `1.2`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.curve": {
      "type": "string",
      "description": "Exponent on each band's level: 1 is linear, below 1 lifts quiet detail, above 1 pushes it down.",
      "markdownDescription": "Exponent on each band's level: 1 is linear, below 1 lifts quiet detail, above 1 pushes it down.\n\n- default `1.0`\n- typical range `0.05` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.radius.spectrum": {
      "type": "string",
      "description": "Radius of the ring the readout is drawn around, in the radial layouts.",
      "markdownDescription": "Radius of the ring the readout is drawn around, in the radial layouts.\n\n- default `0.35`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.span": {
      "type": "string",
      "description": "How much of the frequency axis is shown; below 1 the top end is cut.",
      "markdownDescription": "How much of the frequency axis is shown; below 1 the top end is cut.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.baseline": {
      "type": "string",
      "description": "Where the flat layout's zero line sits vertically.",
      "markdownDescription": "Where the flat layout's zero line sits vertically.\n\n- default `-0.85`\n- no typical range declared\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.rotation.spectrum": {
      "type": "string",
      "description": "Turns the readout, in radians.",
      "markdownDescription": "Turns the readout, in radians.\n\n- default `0.0`\n- typical range `0.0` to `6.2831855` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.thickness.spectrum": {
      "type": "string",
      "description": "Stroke width in pixels at the render target, before softness widens the falloff.",
      "markdownDescription": "Stroke width in pixels at the render target, before softness widens the falloff.\n\n- default `6.0`\n- typical range `0.5` to `12.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue.spectrum": {
      "type": "string",
      "description": "Where this scene reads from the palette, as a coordinate along it rather than a colour.",
      "markdownDescription": "Where this scene reads from the palette, as a coordinate along it rather than a colour.\n\n- default `0.55`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.spawn_rate": {
      "type": "string",
      "description": "New objects launched per second.",
      "markdownDescription": "New objects launched per second.\n\n- default `120.0`\n- typical range `0.0` to `2000.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.gravity": {
      "type": "string",
      "description": "Downward acceleration, in frame heights per second squared; negative floats them up.",
      "markdownDescription": "Downward acceleration, in frame heights per second squared; negative floats them up.\n\n- default `1.5`\n- typical range `-4.0` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.launch_speed": {
      "type": "string",
      "description": "Speed each object leaves the source at.",
      "markdownDescription": "Speed each object leaves the source at.\n\n- default `1.75`\n- typical range `0.0` to `6.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.launch_angle": {
      "type": "string",
      "description": "Direction of launch, in radians clockwise from straight up.",
      "markdownDescription": "Direction of launch, in radians clockwise from straight up.\n\n- default `0.0`\n- typical range `-3.1415927` to `3.1415927` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.spread": {
      "type": "string",
      "description": "How wide the launch directions fan out about that angle.",
      "markdownDescription": "How wide the launch directions fan out about that angle.\n\n- default `0.55`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.lifetime": {
      "type": "string",
      "description": "Seconds an object lives before it fades out.",
      "markdownDescription": "Seconds an object lives before it fades out.\n\n- default `3.0`\n- typical range `0.1` to `20.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.lifetime_spread": {
      "type": "string",
      "description": "How much lifetimes vary between objects; 0 makes them all die together.",
      "markdownDescription": "How much lifetimes vary between objects; 0 makes them all die together.\n\n- default `0.45`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.source_y": {
      "type": "string",
      "description": "Height the source sits at, which is normally just below the frame.",
      "markdownDescription": "Height the source sits at, which is normally just below the frame.\n\n- default `-1.12`\n- no typical range declared\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.source_width": {
      "type": "string",
      "description": "How wide a line the objects are launched from; 0 is a single point.",
      "markdownDescription": "How wide a line the objects are launched from; 0 is a single point.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.spawn_fade": {
      "type": "string",
      "description": "Fades each object in over the start of its life rather than popping it on.",
      "markdownDescription": "Fades each object in over the start of its life rather than popping it on.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.prewarm": {
      "type": "string",
      "description": "Back-dates the population so the first frame is already the steady state.",
      "markdownDescription": "Back-dates the population so the first frame is already the steady state.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.size.emitter": {
      "type": "string",
      "description": "Size of each object's mark.",
      "markdownDescription": "Size of each object's mark.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.size_spread.emitter": {
      "type": "string",
      "description": "How much sizes vary between objects.",
      "markdownDescription": "How much sizes vary between objects.\n\n- default `0.6`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.spin.emitter": {
      "type": "string",
      "description": "Turns per second each object rotates by as it flies.",
      "markdownDescription": "Turns per second each object rotates by as it flies.\n\n- default `0.0`\n- typical range `-4.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.twinkle.emitter": {
      "type": "string",
      "description": "Per-object brightness flicker, seeded so it is reproducible.",
      "markdownDescription": "Per-object brightness flicker, seeded so it is reproducible.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.hue_spread.emitter": {
      "type": "string",
      "description": "How far across the palette the object colours reach.",
      "markdownDescription": "How far across the palette the object colours reach.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.scale.shape_field": {
      "type": "string",
      "description": "Size of the shape within the frame.",
      "markdownDescription": "Size of the shape within the frame.\n\n- default `0.6`\n- typical range `0.05` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_span.shape_field": {
      "type": "string",
      "description": "How much of the palette the field's range covers.",
      "markdownDescription": "How much of the palette the field's range covers.\n\n- default `0.6`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_center.shape_field": {
      "type": "string",
      "description": "Shifts which part of that range lands in the middle of the palette.",
      "markdownDescription": "Shifts which part of that range lands in the middle of the palette.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.gamma.shape_field": {
      "type": "string",
      "description": "Shapes the falloff from the shape's edge; below 1 it bites sooner.",
      "markdownDescription": "Shapes the falloff from the shape's edge; below 1 it bites sooner.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.coord_mode": {
      "type": "string",
      "description": "Which coordinate frame the distance is measured in, which changes the shape's whole geometry.",
      "markdownDescription": "Which coordinate frame the distance is measured in, which changes the shape's whole geometry.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.rotation.shape_field": {
      "type": "string",
      "description": "Turns the shape, in radians.",
      "markdownDescription": "Turns the shape, in radians.\n\n- default `0.0`\n- typical range `0.0` to `6.2831855` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.stroke": {
      "type": "string",
      "description": "Draws the outline instead of the filled figure, at this half-width; 0 fills.",
      "markdownDescription": "Draws the outline instead of the filled figure, at this half-width; 0 fills.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.morph.shape_field": {
      "type": "string",
      "description": "Travels the authored path towards its morph_to silhouette; inert without one.",
      "markdownDescription": "Travels the authored path towards its morph_to silhouette; inert without one.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.zoom.warp_mesh": {
      "type": "string",
      "description": "Scale the previous frame is resampled at, per vertex; above 1 the image tunnels inward.",
      "markdownDescription": "Scale the previous frame is resampled at, per vertex; above 1 the image tunnels inward.\n\n- default `1.0`\n- typical range `0.5` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.rot": {
      "type": "string",
      "description": "Turns per second the resample is rotated by, per vertex.",
      "markdownDescription": "Turns per second the resample is rotated by, per vertex.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.cx": {
      "type": "string",
      "description": "Horizontal point the per-vertex zoom and rotation pivot about, in uv.",
      "markdownDescription": "Horizontal point the per-vertex zoom and rotation pivot about, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.cy": {
      "type": "string",
      "description": "Vertical point the per-vertex zoom and rotation pivot about, in uv.",
      "markdownDescription": "Vertical point the per-vertex zoom and rotation pivot about, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.dx": {
      "type": "string",
      "description": "Sideways offset of the resample, in frame widths.",
      "markdownDescription": "Sideways offset of the resample, in frame widths.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.dy": {
      "type": "string",
      "description": "Vertical offset of the resample, in frame heights.",
      "markdownDescription": "Vertical offset of the resample, in frame heights.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.sx": {
      "type": "string",
      "description": "Horizontal stretch of the resample, independently of `zoom`.",
      "markdownDescription": "Horizontal stretch of the resample, independently of `zoom`.\n\n- default `1.0`\n- typical range `0.5` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.sy": {
      "type": "string",
      "description": "Vertical stretch of the resample, independently of `zoom`.",
      "markdownDescription": "Vertical stretch of the resample, independently of `zoom`.\n\n- default `1.0`\n- typical range `0.5` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.warp.warp_mesh": {
      "type": "string",
      "description": "Amplitude of the travelling ripple added to the resample.",
      "markdownDescription": "Amplitude of the travelling ripple added to the resample.\n\n- default `0.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.warp_scale": {
      "type": "string",
      "description": "Spatial frequency of the ripple; higher makes it finer.",
      "markdownDescription": "Spatial frequency of the ripple; higher makes it finer.\n\n- default `1.0`\n- typical range `0.1` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.warp_speed": {
      "type": "string",
      "description": "How fast the ripple travels, as a multiple of its base rate.",
      "markdownDescription": "How fast the ripple travels, as a multiple of its base rate.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.decay.warp_mesh": {
      "type": "string",
      "description": "How much of the field survives each second, which is what sets the trail's length.",
      "markdownDescription": "How much of the field survives each second, which is what sets the trail's length.\n\n- default `0.72`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.deposit": {
      "type": "string",
      "description": "How much light the source figure adds into the field each frame.",
      "markdownDescription": "How much light the source figure adds into the field each frame.\n\n- default `1.6`\n- typical range `0.0` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.deposit_x": {
      "type": "string",
      "description": "Horizontal position of the deposited figure, in uv.",
      "markdownDescription": "Horizontal position of the deposited figure, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.deposit_y": {
      "type": "string",
      "description": "Vertical position of the deposited figure, in uv.",
      "markdownDescription": "Vertical position of the deposited figure, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.deposit_radius": {
      "type": "string",
      "description": "Radius of the deposited ring.",
      "markdownDescription": "Radius of the deposited ring.\n\n- default `0.45`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.deposit_width": {
      "type": "string",
      "description": "How thick that ring is; narrow reads as a wire, wide as a disc.",
      "markdownDescription": "How thick that ring is; narrow reads as a wire, wide as a disc.\n\n- default `0.11`\n- typical range `0.0` to `0.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.deposit_arms": {
      "type": "string",
      "description": "How many arms the ring is broken into, as a whole number of arms; 0 leaves it whole.",
      "markdownDescription": "How many arms the ring is broken into, as a whole number of arms; 0 leaves it whole.\n\n- default `0.0`\n- typical range `0.0` to `16.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.deposit_twist": {
      "type": "string",
      "description": "Sweeps the arms into a spiral rather than leaving them radial.",
      "markdownDescription": "Sweeps the arms into a spiral rather than leaving them radial.\n\n- default `0.0`\n- typical range `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.deposit_spin": {
      "type": "string",
      "description": "Turns per second the deposited figure rotates by.",
      "markdownDescription": "Turns per second the deposited figure rotates by.\n\n- default `0.0`\n- typical range `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.gamma.warp_mesh": {
      "type": "string",
      "description": "Shapes the field's tone curve on its way out; below 1 lifts the mid tones.",
      "markdownDescription": "Shapes the field's tone curve on its way out; below 1 lifts the mid tones.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.wrap": {
      "type": "string",
      "description": "Wraps a sample that leaves the frame back in at the opposite edge, instead of clamping.",
      "markdownDescription": "Wraps a sample that leaves the frame back in at the opposite edge, instead of clamping.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.darken_center": {
      "type": "string",
      "description": "Pulls brightness down toward the middle of the frame.",
      "markdownDescription": "Pulls brightness down toward the middle of the frame.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.brighten": {
      "type": "string",
      "description": "Lifts the field's bright end, MilkDrop's own brighten switch.",
      "markdownDescription": "Lifts the field's bright end, MilkDrop's own brighten switch.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.darken": {
      "type": "string",
      "description": "Pushes the field's dark end down, MilkDrop's own darken switch.",
      "markdownDescription": "Pushes the field's dark end down, MilkDrop's own darken switch.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.solarize": {
      "type": "string",
      "description": "Inverts the field above its midpoint, so highlights fold back into shadow.",
      "markdownDescription": "Inverts the field above its midpoint, so highlights fold back into shadow.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.invert": {
      "type": "string",
      "description": "Inverts the whole field.",
      "markdownDescription": "Inverts the whole field.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.echo_alpha": {
      "type": "string",
      "description": "How strongly a second, scaled copy of the field is blended over the first.",
      "markdownDescription": "How strongly a second, scaled copy of the field is blended over the first.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.echo_zoom": {
      "type": "string",
      "description": "How much larger or smaller that echoed copy is.",
      "markdownDescription": "How much larger or smaller that echoed copy is.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.echo_orient": {
      "type": "string",
      "description": "Which way the echoed copy is flipped before it is blended.",
      "markdownDescription": "Which way the echoed copy is flipped before it is blended.\n\n- default `0.0`\n- typical range `0.0` to `3.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_span.warp_mesh": {
      "type": "string",
      "description": "How much of the palette the field's range covers.",
      "markdownDescription": "How much of the palette the field's range covers.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_source": {
      "type": "string",
      "description": "Where the field takes its colour: 0 the deposit's own angle, 1 the light it has built up.",
      "markdownDescription": "Where the field takes its colour: 0 the deposit's own angle, 1 the light it has built up.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.count": {
      "type": "string",
      "description": "How many elements are placed; 0 lets the layout decide. Truncated, so a rise admits its next element on arrival.",
      "markdownDescription": "How many elements are placed; 0 lets the layout decide. Truncated, so a rise admits its next element on arrival.\n\n- default `0.0`\n- typical range `0.0` to `64.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.layout": {
      "type": "string",
      "description": "Picks which arrangement the elements are placed by.",
      "markdownDescription": "Picks which arrangement the elements are placed by.\n\n- default `0.0`\n- typical range `0.0` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.seed": {
      "type": "string",
      "description": "Chooses one arrangement out of the family; the same seed always composes the same way. Truncated, like `count`.",
      "markdownDescription": "Chooses one arrangement out of the family; the same seed always composes the same way. Truncated, like `count`.\n\n- default `0.0`\n- no typical range declared\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.size_hierarchy": {
      "type": "string",
      "description": "How much larger the leading elements are than the rest; 0 makes them equal.",
      "markdownDescription": "How much larger the leading elements are than the rest; 0 makes them equal.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.angle_bias": {
      "type": "string",
      "description": "Degrees the elements lean by, which is what gives the composition its tilt.",
      "markdownDescription": "Degrees the elements lean by, which is what gives the composition its tilt.\n\n- default `-22.0`\n- no typical range declared\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.roster": {
      "type": "string",
      "description": "Picks which set of shapes the elements are drawn from.",
      "markdownDescription": "Picks which set of shapes the elements are drawn from.\n\n- default `0.0`\n- typical range `0.0` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.density": {
      "type": "string",
      "description": "How much of the frame the arrangement fills.",
      "markdownDescription": "How much of the frame the arrangement fills.\n\n- default `1.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.drift": {
      "type": "string",
      "description": "How far the elements wander from their placed positions.",
      "markdownDescription": "How far the elements wander from their placed positions.\n\n- default `0.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.spin.shape_collage": {
      "type": "string",
      "description": "Turns per second the elements rotate by.",
      "markdownDescription": "Turns per second the elements rotate by.\n\n- default `0.0`\n- typical range `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.recompose": {
      "type": "string",
      "description": "Crossing zero lays the composition out again from a new arrangement.",
      "markdownDescription": "Crossing zero lays the composition out again from a new arrangement.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.recompose_blend": {
      "type": "string",
      "description": "How long the change between two arrangements takes, rather than cutting.",
      "markdownDescription": "How long the change between two arrangements takes, rather than cutting.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.pump_size": {
      "type": "string",
      "description": "Scales every element together, for a beat to make the whole composition breathe.",
      "markdownDescription": "Scales every element together, for a beat to make the whole composition breathe.\n\n- default `0.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.pump_alpha": {
      "type": "string",
      "description": "Fades every element together, the opacity twin of `pump_size`.",
      "markdownDescription": "Fades every element together, the opacity twin of `pump_size`.\n\n- default `0.0`\n- typical range `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.scale.shape_collage": {
      "type": "string",
      "description": "Size of the whole composition within the frame.",
      "markdownDescription": "Size of the whole composition within the frame.\n\n- default `1.0`\n- typical range `0.1` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.paper": {
      "type": "string",
      "description": "How opaque the ground behind the elements is; 0 leaves the backdrop showing.",
      "markdownDescription": "How opaque the ground behind the elements is; 0 leaves the backdrop showing.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_span.shape_collage": {
      "type": "string",
      "description": "How much of the palette the elements are coloured across.",
      "markdownDescription": "How much of the palette the elements are coloured across.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.palette_shift": {
      "type": "string",
      "description": "Rotates every element's colour along the palette together.",
      "markdownDescription": "Rotates every element's colour along the palette together.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.opacity": {
      "type": "string",
      "description": "How opaque each element is, so overlaps can show through.",
      "markdownDescription": "How opaque each element is, so overlaps can show through.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.edge_softness": {
      "type": "string",
      "description": "How far each element's edge fades; 0 is a hard cut.",
      "markdownDescription": "How far each element's edge fades; 0 is a hard cut.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.mode_n": {
      "type": "string",
      "description": "The plate's first mode number: how many nodal lines cross one axis. Equal to `mode_m`, the two waves cancel and the plate is blank.",
      "markdownDescription": "The plate's first mode number: how many nodal lines cross one axis. Equal to `mode_m`, the two waves cancel and the plate is blank.\n\n- default `3.0`\n- typical range depends on the family: `chladni` `1.0` to `16.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `escape_time` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.mode_m": {
      "type": "string",
      "description": "The plate's second mode number: how many nodal lines cross the other axis.",
      "markdownDescription": "The plate's second mode number: how many nodal lines cross the other axis.\n\n- default `5.0`\n- typical range depends on the family: `chladni` `1.0` to `16.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `escape_time` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.line_width": {
      "type": "string",
      "description": "How wide a band around the nodal lines lights, in plate units (the plate is 2 across); 0 is a one-pixel line.",
      "markdownDescription": "How wide a band around the nodal lines lights, in plate units (the plate is 2 across); 0 is a one-pixel line.\n\n- default `0.04`\n- typical range depends on the family: `chladni` `0.0` to `0.3` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `escape_time` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.plate_mix": {
      "type": "string",
      "description": "Blends from the nodal lines alone toward the whole signed wave, which reads as a standing wave rather than as sand.",
      "markdownDescription": "Blends from the nodal lines alone toward the whole signed wave, which reads as a standing wave rather than as sand.\n\n- default `0.0`\n- typical range depends on the family: `chladni` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `escape_time` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.iterations": {
      "type": "string",
      "description": "How many steps an orbit is followed before it is called part of the set; more resolves finer boundary detail. Capped by the quality tier.",
      "markdownDescription": "How many steps an orbit is followed before it is called part of the set; more resolves finer boundary detail. Capped by the quality tier.\n\n- default `64.0`\n- typical range depends on the family: `escape_time` `1.0` to `512.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `chladni` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.c_re": {
      "type": "string",
      "description": "The real part of the Julia constant: the lever that reshapes the set, from one connected piece to dust. Inert on the `mandelbrot` map.",
      "markdownDescription": "The real part of the Julia constant: the lever that reshapes the set, from one connected piece to dust. Inert on the `mandelbrot` map.\n\n- default `-0.8`\n- typical range depends on the family: `escape_time` `-1.5` to `0.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `chladni` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.c_im": {
      "type": "string",
      "description": "The imaginary part of the Julia constant. Inert on the `mandelbrot` map.",
      "markdownDescription": "The imaginary part of the Julia constant. Inert on the `mandelbrot` map.\n\n- default `0.156`\n- typical range depends on the family: `escape_time` `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `chladni` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.escape_radius": {
      "type": "string",
      "description": "How far an orbit must travel to count as escaped; larger smooths the colour bands' spacing.",
      "markdownDescription": "How far an orbit must travel to count as escaped; larger smooths the colour bands' spacing.\n\n- default `16.0`\n- typical range depends on the family: `escape_time` `2.0` to `256.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `chladni` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.power": {
      "type": "string",
      "description": "The exponent in z -> z^power + c: 2 is the classic set, higher whole powers add lobes, and a fractional power tears along the negative real axis.",
      "markdownDescription": "The exponent in z -> z^power + c: 2 is the classic set, higher whole powers add lobes, and a fractional power tears along the negative real axis.\n\n- default `2.0`\n- typical range depends on the family: `escape_time` `1.5` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `chladni` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.interior": {
      "type": "string",
      "description": "How much light the set itself emits; 0 is the textbook black interior.",
      "markdownDescription": "How much light the set itself emits; 0 is the textbook black interior.\n\n- default `0.0`\n- typical range depends on the family: `escape_time` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `chladni` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.trap_radius": {
      "type": "string",
      "description": "How far the orbit trap sits from the origin — the circle's radius, the line's offset, the point's and the cross's distance. Inert with no `trap`.",
      "markdownDescription": "How far the orbit trap sits from the origin — the circle's radius, the line's offset, the point's and the cross's distance. Inert with no `trap`.\n\n- default `0.5`\n- typical range depends on the family: `escape_time` `0.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `chladni` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.trap_rotate": {
      "type": "string",
      "description": "Turns the orbit trap about the origin, in whole turns. Inert on a `circle` and with no `trap`.",
      "markdownDescription": "Turns the orbit trap about the origin, in whole turns. Inert on a `circle` and with no `trap`.\n\n- default `0.0`\n- typical range depends on the family: `escape_time` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `chladni` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_span.analytic_field": {
      "type": "string",
      "description": "How much of the palette the field's level covers; 0 is one flat colour.",
      "markdownDescription": "How much of the palette the field's level covers; 0 is one flat colour.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.color_center.analytic_field": {
      "type": "string",
      "description": "Shifts which part of the palette the field's level starts from.",
      "markdownDescription": "Shifts which part of the palette the field's level starts from.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.birth": {
      "type": "string",
      "description": "Which live-neighbour counts bring a dead cell to life, as a bitmask over the counts 0-8: bit k set means k neighbours give birth. 8 (bit 3) is Conway's.",
      "markdownDescription": "Which live-neighbour counts bring a dead cell to life, as a bitmask over the counts 0-8: bit k set means k neighbours give birth. 8 (bit 3) is Conway's.\n\n- default `8.0`\n- typical range depends on the family: `life_like` `0.0` to `511.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `larger_than_life`, `cyclic` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.survive": {
      "type": "string",
      "description": "Which live-neighbour counts keep a live cell alive, as a bitmask over the counts 0-8. 12 (bits 2 and 3) is Conway's.",
      "markdownDescription": "Which live-neighbour counts keep a live cell alive, as a bitmask over the counts 0-8. 12 (bits 2 and 3) is Conway's.\n\n- default `12.0`\n- typical range depends on the family: `life_like` `0.0` to `511.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `larger_than_life`, `cyclic` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.radius.cellular": {
      "type": "string",
      "description": "How far the neighbourhood reaches, in cells: a square of side 2 x radius + 1 about each cell. Capped by the quality tier.",
      "markdownDescription": "How far the neighbourhood reaches, in cells: a square of side 2 x radius + 1 about each cell. Capped by the quality tier.\n\n- default `5.0`\n- typical range depends on the family: `larger_than_life` `1.0` to `10.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `life_like`, `cyclic` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.birth_lo": {
      "type": "string",
      "description": "The least filled fraction of its neighbourhood at which a dead cell is born.",
      "markdownDescription": "The least filled fraction of its neighbourhood at which a dead cell is born.\n\n- default `0.28`\n- typical range depends on the family: `larger_than_life` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `life_like`, `cyclic` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.birth_hi": {
      "type": "string",
      "description": "The most filled fraction of its neighbourhood at which a dead cell is born.",
      "markdownDescription": "The most filled fraction of its neighbourhood at which a dead cell is born.\n\n- default `0.385`\n- typical range depends on the family: `larger_than_life` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `life_like`, `cyclic` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.survive_lo": {
      "type": "string",
      "description": "The least filled fraction of its neighbourhood at which a live cell survives.",
      "markdownDescription": "The least filled fraction of its neighbourhood at which a live cell survives.\n\n- default `0.26`\n- typical range depends on the family: `larger_than_life` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `life_like`, `cyclic` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.survive_hi": {
      "type": "string",
      "description": "The most filled fraction of its neighbourhood at which a live cell survives.",
      "markdownDescription": "The most filled fraction of its neighbourhood at which a live cell survives.\n\n- default `0.47`\n- typical range depends on the family: `larger_than_life` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `life_like`, `cyclic` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.states": {
      "type": "string",
      "description": "How many colours the cycle holds; each cell advances to the next one round it.",
      "markdownDescription": "How many colours the cycle holds; each cell advances to the next one round it.\n\n- default `3.0`\n- typical range depends on the family: `cyclic` `2.0` to `24.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `life_like`, `larger_than_life` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.threshold": {
      "type": "string",
      "description": "How many of its eight neighbours must already hold the next colour before a cell advances to it.",
      "markdownDescription": "How many of its eight neighbours must already hold the next colour before a cell advances to it.\n\n- default `3.0`\n- typical range depends on the family: `cyclic` `1.0` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `life_like`, `larger_than_life` — the parameter is not read there at all\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.step_rate": {
      "type": "string",
      "description": "How many generations the automaton runs per second, whatever the frame rate; 0 freezes it.",
      "markdownDescription": "How many generations the automaton runs per second, whatever the frame rate; 0 freezes it.\n\n- default `10.0`\n- typical range `0.0` to `60.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.reseed.cellular": {
      "type": "string",
      "description": "A rise past 0.5 refills one disc of the grid with fresh seeded cells, once per rise; bind a beat or a latch to it.",
      "markdownDescription": "A rise past 0.5 refills one disc of the grid with fresh seeded cells, once per rise; bind a beat or a latch to it.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.trail": {
      "type": "string",
      "description": "How many generations a dead cell keeps glowing, fading as it goes; 0 draws only the live cells.",
      "markdownDescription": "How many generations a dead cell keeps glowing, fading as it goes; 0 draws only the live cells.\n\n- default `12.0`\n- typical range depends on the family: `life_like` `0.0` to `64.0`, `larger_than_life` `0.0` to `64.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `cyclic` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.age_tint": {
      "type": "string",
      "description": "How far along the palette a dead cell's glow travels as it fades; 0 keeps the wake the live cells' colour.",
      "markdownDescription": "How far along the palette a dead cell's glow travels as it fades; 0 keeps the wake the live cells' colour.\n\n- default `0.35`\n- typical range depends on the family: `life_like` `0.0` to `1.0`, `larger_than_life` `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- inert on `cyclic` — the parameter is not read there at all\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_hue": {
      "type": "string",
      "description": "Where the backdrop starts in the preset's palette, as a coordinate along it.",
      "markdownDescription": "Where the backdrop starts in the preset's palette, as a coordinate along it.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_bright": {
      "type": "string",
      "description": "How lit the backdrop is; 0 is black and the scene draws on nothing.",
      "markdownDescription": "How lit the backdrop is; 0 is black and the scene draws on nothing.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_vignette": {
      "type": "string",
      "description": "Darkens the backdrop toward the corners, pulling the eye to the middle.",
      "markdownDescription": "Darkens the backdrop toward the corners, pulling the eye to the middle.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_angle": {
      "type": "string",
      "description": "Direction the backdrop ramp runs in, in radians; 0 runs bottom to top.",
      "markdownDescription": "Direction the backdrop ramp runs in, in radians; 0 runs bottom to top.\n\n- default `0.0`\n- typical range `0.0` to `6.2831855` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_hue_span": {
      "type": "string",
      "description": "How far along the palette the ramp travels from `bg_hue`; 0 is a flat colour.",
      "markdownDescription": "How far along the palette the ramp travels from `bg_hue`; 0 is a flat colour.\n\n- default `0.0`\n- typical range `-0.5` to `0.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_shade": {
      "type": "string",
      "description": "Brightness multiplier at the ramp's start, so a sky can be dark at one edge.",
      "markdownDescription": "Brightness multiplier at the ramp's start, so a sky can be dark at one edge.\n\n- default `0.72`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_shade_end": {
      "type": "string",
      "description": "Brightness multiplier at the ramp's far end.",
      "markdownDescription": "Brightness multiplier at the ramp's far end.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_ramp_gamma": {
      "type": "string",
      "description": "Bends the ramp's progress: below 1 the far colour arrives early, above 1 it holds off.",
      "markdownDescription": "Bends the ramp's progress: below 1 the far colour arrives early, above 1 it holds off.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_band_amount": {
      "type": "string",
      "description": "Strength of a second colour band laid across the ramp; 0 removes it.",
      "markdownDescription": "Strength of a second colour band laid across the ramp; 0 removes it.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_band_angle": {
      "type": "string",
      "description": "Direction the band runs in, independently of the ramp, in radians.",
      "markdownDescription": "Direction the band runs in, independently of the ramp, in radians.\n\n- default `0.0`\n- typical range `0.0` to `6.2831855` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_band_pos": {
      "type": "string",
      "description": "Where across the frame the band sits.",
      "markdownDescription": "Where across the frame the band sits.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_band_width": {
      "type": "string",
      "description": "How wide the band is; narrow reads as a horizon, wide as a wash.",
      "markdownDescription": "How wide the band is; narrow reads as a horizon, wide as a wash.\n\n- default `0.15`\n- typical range `0.02` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_band_curve": {
      "type": "string",
      "description": "Bows the band into an arc instead of a straight line.",
      "markdownDescription": "Bows the band into an arc instead of a straight line.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_band_hue": {
      "type": "string",
      "description": "Where the band's own colour is picked from the palette.",
      "markdownDescription": "Where the band's own colour is picked from the palette.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bg_band_hue_span": {
      "type": "string",
      "description": "How far the band's colour travels along the palette across its width.",
      "markdownDescription": "How far the band's colour travels along the palette across its width.\n\n- default `0.0`\n- typical range `-0.5` to `0.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.trails": {
      "type": "string",
      "description": "How much of the previous frame survives into this one; 0 is no trail, near 1 a long smear.",
      "markdownDescription": "How much of the previous frame survives into this one; 0 is no trail, near 1 a long smear.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_zoom.trails": {
      "type": "string",
      "description": "Scale the accumulation is grown by each second, so held above 1 the trail tunnels outward.",
      "markdownDescription": "Scale the accumulation is grown by each second, so held above 1 the trail tunnels outward.\n\n- default `1.0`\n- typical range `0.9` to `1.1` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_rotate.trails": {
      "type": "string",
      "description": "Turns per second the accumulation is rotated by, about the feedback centre.",
      "markdownDescription": "Turns per second the accumulation is rotated by, about the feedback centre.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_dx.trails": {
      "type": "string",
      "description": "Sideways drift of the accumulation, in frame widths per second.",
      "markdownDescription": "Sideways drift of the accumulation, in frame widths per second.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_dy.trails": {
      "type": "string",
      "description": "Vertical drift of the accumulation, in frame heights per second.",
      "markdownDescription": "Vertical drift of the accumulation, in frame heights per second.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_center_x.trails": {
      "type": "string",
      "description": "The horizontal point the zoom and the rotation pivot about, in uv.",
      "markdownDescription": "The horizontal point the zoom and the rotation pivot about, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_center_y.trails": {
      "type": "string",
      "description": "The vertical point the zoom and the rotation pivot about, in uv.",
      "markdownDescription": "The vertical point the zoom and the rotation pivot about, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.fb_warp.trails": {
      "type": "string",
      "description": "Amplitude of a swirl added to the feedback sample, so the trail curls rather than sliding.",
      "markdownDescription": "Amplitude of a swirl added to the feedback sample, so the trail curls rather than sliding.\n\n- default `0.0`\n- typical range `0.0` to `0.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_order": {
      "type": "string",
      "description": "How many mirrored wedges the frame is folded into; 1 is no fold at all.",
      "markdownDescription": "How many mirrored wedges the frame is folded into; 1 is no fold at all.\n\n- default `1.0`\n- typical range `1.0` to `16.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_angle": {
      "type": "string",
      "description": "Rotates the whole fold, in radians.",
      "markdownDescription": "Rotates the whole fold, in radians.\n\n- default `0.0`\n- typical range `0.0` to `6.2831855` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_center_x": {
      "type": "string",
      "description": "The horizontal point the wedges radiate from, in uv.",
      "markdownDescription": "The horizontal point the wedges radiate from, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_center_y": {
      "type": "string",
      "description": "The vertical point the wedges radiate from, in uv.",
      "markdownDescription": "The vertical point the wedges radiate from, in uv.\n\n- default `0.5`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_edge": {
      "type": "string",
      "description": "Softens the seam between mirrored wedges; 1 is a hard edge.",
      "markdownDescription": "Softens the seam between mirrored wedges; 1 is a hard edge.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- structural parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_tile": {
      "type": "string",
      "description": "Repeats the source across the frame before it is folded, so one wedge shows several copies.",
      "markdownDescription": "Repeats the source across the frame before it is folded, so one wedge shows several copies.\n\n- default `1.0`\n- typical range `1.0` to `8.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_radial": {
      "type": "string",
      "description": "Scales distance from the centre when sampling, pulling detail inward or pushing it out.",
      "markdownDescription": "Scales distance from the centre when sampling, pulling detail inward or pushing it out.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_spiral": {
      "type": "string",
      "description": "Rotates the sample by an amount that grows with radius, turning the wedges into a spiral.",
      "markdownDescription": "Rotates the sample by an amount that grows with radius, turning the wedges into a spiral.\n\n- default `0.0`\n- typical range `-2.0` to `2.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_zoom": {
      "type": "string",
      "description": "Shifts the sampled radius inward or outward, riding on the radial term.",
      "markdownDescription": "Shifts the sampled radius inward or outward, riding on the radial term.\n\n- default `0.0`\n- typical range `-1.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.kaleido_inner": {
      "type": "string",
      "description": "Radius of the untouched disc at the centre, which keeps the pivot from smearing.",
      "markdownDescription": "Radius of the untouched disc at the centre, which keeps the pivot from smearing.\n\n- default `0.06`\n- typical range `0.0` to `0.5` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bloom_amount": {
      "type": "string",
      "description": "How much of the blurred bright pass is added back; 0 turns the stage off entirely.",
      "markdownDescription": "How much of the blurred bright pass is added back; 0 turns the stage off entirely.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bloom_threshold": {
      "type": "string",
      "description": "The linear level a pixel must exceed before it glows at all; raise it to bloom only highlights.",
      "markdownDescription": "The linear level a pixel must exceed before it glows at all; raise it to bloom only highlights.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.bloom_radius": {
      "type": "string",
      "description": "Scales how far the glow spreads from the pixel that produced it.",
      "markdownDescription": "Scales how far the glow spreads from the pixel that produced it.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.occlude": {
      "type": "string",
      "description": "How much of the backdrop the scene's own coverage hides; 0 lets the sky through everywhere.",
      "markdownDescription": "How much of the backdrop the scene's own coverage hides; 0 lets the sky through everywhere.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.exposure": {
      "type": "string",
      "description": "Linear gain applied to the whole frame before the tonemap; 1 leaves it as rendered.",
      "markdownDescription": "Linear gain applied to the whole frame before the tonemap; 1 leaves it as rendered.\n\n- default `1.0`\n- typical range `0.0` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.ink_amount": {
      "type": "string",
      "description": "How far the frame is remapped onto the paper-and-ink pair; 0 leaves it untouched.",
      "markdownDescription": "How far the frame is remapped onto the paper-and-ink pair; 0 leaves it untouched.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.paper_hue": {
      "type": "string",
      "description": "Hue of the colour an unlit pixel becomes, as a position around the wheel.",
      "markdownDescription": "Hue of the colour an unlit pixel becomes, as a position around the wheel.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.paper_sat": {
      "type": "string",
      "description": "Saturation of the paper colour; 0 is neutral.",
      "markdownDescription": "Saturation of the paper colour; 0 is neutral.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.paper_bright": {
      "type": "string",
      "description": "Brightness of the paper colour, which sets how light the empty ground reads.",
      "markdownDescription": "Brightness of the paper colour, which sets how light the empty ground reads.\n\n- default `1.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.ink_hue": {
      "type": "string",
      "description": "Hue of the colour a fully lit pixel becomes.",
      "markdownDescription": "Hue of the colour a fully lit pixel becomes.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.ink_sat": {
      "type": "string",
      "description": "Saturation of the ink colour; 0 is neutral.",
      "markdownDescription": "Saturation of the ink colour; 0 is neutral.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.ink_bright": {
      "type": "string",
      "description": "Brightness of the ink colour, which sets how dark the drawn marks read.",
      "markdownDescription": "Brightness of the ink colour, which sets how dark the drawn marks read.\n\n- default `0.0`\n- typical range `0.0` to `1.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    },
    "param.ink_gamma": {
      "type": "string",
      "description": "Shapes the paper-to-ink ramp: below 1 the mid tones bite earlier, above 1 they hold back.",
      "markdownDescription": "Shapes the paper-to-ink ramp: below 1 the mid tones bite earlier, above 1 they hold back.\n\n- default `1.0`\n- typical range `0.25` to `4.0` — a guide, not a bound: the engine neither clamps to it nor rejects a value outside it\n- modal parameter\n\nThe value is an expression, always quoted."
    }
  }
}
